# spec_board.json (board view-state: collapse + manual node positions) is now
# version-controlled so the curated arrangement is shared/committed.
# (Tree mode stores only {collapsed, positions} — no image dataURLs.)
