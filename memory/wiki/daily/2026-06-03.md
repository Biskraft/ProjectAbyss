# 2026-06-03

- Extended `WorldItemDropRuntime` to own item-drop collection side effects, moving inventory add, item-collected stat increment, localized acquisition toast, persistent item-key marking, pickup glow, and sacred-pickup handoff out of `LdtkWorldScene` while keeping backing inventory/progress/save objects callback-owned. Verification: `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldVoidReturnRuntime` for LDtk world void return-point resolution and teleport placement, moving last-safe fallback validation, builder-volume/stamp exclusion, nearest floor/body-clear search, LDtk Player fallback, full-black teleport placement, landed-pose forcing, and camera snap out of `LdtkWorldScene` while leaving fade/input-lock timing in `WorldVoidRuntime`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldEnemySpawnRuntime` for LDtk world enemy spawn parsing and placement, moving direct `Slime`/`Boss` entity spawn, `Enemy_Spawn` factory use, persistent boss-key skips, boss-lock activation decisions, roomData/target assignment, and target-door metadata out of `LdtkWorldScene` while keeping enemy update/combat/reward policy scene-owned. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldPlayerImpactRuntime` for LDtk world player surge-contact and dive-landing gameplay side effects, moving dive landing tier rules, combat feedback, enemy area damage/death handling, and cracked-floor/growing-wall surge/dive dispatch out of `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldContainerAttackRuntime` for LDtk world player sword-hit checks against throwable containers, moving active attack hitbox overlap, MetalCrate sword-hit immunity spark, container damage, fluid paint callback, destruction VFX callback, and registry removal out of `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldEgoShardImpactRuntime` for LDtk world Ego Shard elemental terrain impacts and debug elemental sweeps, moving fire/ice/thunder terrain mutation, debug Shift+1/2/3 sweep hitbox calculation, debug logs, and localized debug toasts out of `LdtkWorldScene` while keeping debug key routing and debug enchant switching scene-owned. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldEgoShardCastRuntime` for LDtk world Ego Shard cast input, debug shipping gate, trajectory preview, shard spend, cast gap, and recovery cooldown ticking. Later same-pass work moved elemental terrain impact effects into `WorldEgoShardImpactRuntime`, leaving `LdtkWorldScene` with debug key routing and debug enchant switching. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldEgoShardProjectileRuntime` for LDtk world Ego Shard flight/update dispatch, moving shard solid predicates, terrain-impact dispatch wiring, post-update container-fluid flush, padded player retrieval, and retrieval cooldown removal out of `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldEgoShardCombatRuntime` for LDtk world Ego Shard enemy/container hit callbacks, moving shard damage/status, enemy kill auto-retrieval, throwable-container shard damage, MetalCrate brittle shatter, and registry-based container removal out of `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldDoorSwitchRegistry` for LDtk world `LockedDoor` / `Switch` lifecycle and per-entity collision-grid mapping, moving door/switch list storage, entity-layer attachment, room-clear cleanup, builder membership checks, and WeakMap ownership out of `LdtkWorldScene` while keeping unlock policy, stat-gate feedback, switch-to-door linking, player-door resolution, analytics, toasts, and builder stamp refresh in the scene. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldProgressState` for LDtk world persistent progress Sets, moving `visitedLevels`, `clearedLevels`, `collectedItems`, `collectedRelics`, and `unlockedEvents` ownership out of `LdtkWorldScene`. Save-load paths now mutate the existing Sets through `replaceFromSave()` instead of replacing Set objects, preserving references held by callbacks and pushed scenes. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldDoorAttackState` for LDtk world locked-door per-attack reject tracking, moving `doorRejectSet` and `lastDoorCheckCombo` out of `LdtkWorldScene` while keeping stat-gate policy, feedback text, analytics, unlock side effects, and builder stamp refresh in the scene. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldFluidContactState` for LDtk world non-water fluid contact transitions, moving `prevPlayerInOtherFluid` and `prevEnemyInOtherFluid` out of `LdtkWorldScene` while keeping fluid detection, splash/impulse VFX, steam puffs, residue timers, and residue hazard policy in the scene. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldSolidifiedWallOverlay` for LDtk world hardened magma wall overlay rendering, moving `solidifiedWallGfx`, `solidifiedWallCells`, and `rebuildSolidifiedWallOverlay()` out of `LdtkWorldScene` while keeping TileMutator mutation policy, wall-layer dirty/rerender timing, and collision-grid mutation in the scene. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldProceduralDecorRuntime` for LDtk world procedural decoration lifecycle, moving `procDecorator` and extra decorator storage out of `LdtkWorldScene` while keeping generation policy, palette filters, render insertion order, and grass burnable registration in the scene. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `WorldCollisionGridRuntime` for LDtk world runtime collision-grid clone ownership. `LdtkWorldScene` now accesses the grid through a getter and room load/restore clone LDtk grids through `cloneFrom()`, leaving active gameplay mutation contracts unchanged. Verification: `npm run build` and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `WorldFluidRuntime` for LDtk world dynamic-fluid manager instance ownership, moving creation and grouped clear/detach of `FluidSystem`, `FluidSpawnerManager`, `FluidCrestFoamManager`, and `FluidResidueManager` out of `LdtkWorldScene` while preserving scene-owned fluid gameplay policy and update order. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `WorldTileMutationRuntime` for LDtk world dynamic tile-mutation state, moving `TileMutator`, its overlay renderer, and wall-layer dirty flag out of `LdtkWorldScene` while preserving scene-owned mutation callbacks, hazard policy, rerender timing, and fluid refresh. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `WorldContainerCarryRuntime` for LDtk world throwable-container carry UI/VFX state, moving held/pulling state, lift prompt, pull interpolation origin, and `ArcTether` lifetime out of `LdtkWorldScene` while preserving scene-owned container search, physics/destruction/fluid interactions, and Ego Shard cast suppression. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `WorldPickupVfxRuntime` for LDtk world pickup/acquire VFX managers, moving `ItemPickupGlowManager` and `RelicAuraBurstManager` creation/update/clear out of `LdtkWorldScene` while preserving collection policy in the scene and pickup runtimes. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `WorldMovementVfxRuntime` for LDtk world movement/environment VFX managers, moving creation and grouped updates for landing/dash/jump/wall/footstep/flask/surge/hit/dive/splash/steam/bubble/drop-through/ice-skid effects out of `LdtkWorldScene` while preserving scene-owned spawn timing and gameplay policy. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `WorldCombatFeedbackRuntime` for LDtk world combat feedback managers, moving `DamageNumberManager`, `HitSparkManager`, `PropShatterManager`, and `ScreenFlash` creation/update ownership out of `LdtkWorldScene` while preserving scene-owned combat spawn policy and prior early-return update behavior. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed; diff check only printed existing line-ending warnings.
- Added `WorldStatusFeedbackRuntime` for LDtk world savepoint/low-HP status feedback managers, moving `SavepointPulseManager` and `LowHpVignetteManager` creation/update ownership out of `LdtkWorldScene` while preserving `SavePointRuntime` interaction policy. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed; diff check only printed existing line-ending warnings.
- Added `WorldGrassFireRuntime` for LDtk world procedural grass-fire and ash-remnant managers, moving `GrassClumpFireSystem` and `AshRemnantManager` creation/update ownership out of `LdtkWorldScene` while preserving scene-owned burnable-prop and TileMutator policy. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed; diff check only printed existing line-ending warnings.
- Added `WorldEgoShardRuntime` for LDtk world Ego Shard manager and preview lifetime, moving `EgoShardManager` and `EgoShardPreview` creation/delegation out of `LdtkWorldScene`. Later same-day work split cast, projectile/update, combat hit, and terrain impact policy into dedicated Ego Shard runtimes. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed; diff check only printed existing line-ending warnings.
- Extended `WorldEgoShardRuntime` to own Ego Shard cast charge timing and velocity resolution, removing `egoCastChargeMs` from `LdtkWorldScene` while keeping debug gating and fire/release policy in the scene. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed; diff check only printed existing line-ending warnings.
- Moved the LDtk world anvil prompt suppress timer into `AnvilPromptController`, removing `anvilPromptSuppressMs` from `LdtkWorldScene` while preserving prompt visibility, disabled prompts, and reclaim behavior. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed; diff check only printed existing line-ending warnings.
- Added `WorldContainerFluidState` for the LDtk world container-fluid dirty flag, moving `containerFluidDirty` out of `LdtkWorldScene` while preserving scene-owned container effect policy and flush timing. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed; diff check only printed existing line-ending warnings.
- Removed the unused `LdtkWorldScene.isPaused` flag and made `WorldUiController` pause open/close callbacks optional. Pause state is now derived from `PauseMenu.visible`; behavior remains owned by `PauseMenu`/`WorldUiController`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed; diff check only printed existing line-ending warnings.
- Removed dead `LdtkWorldScene.currentAnvilIid` assignments and the inactive `ControlsOverlay` import/field/commented lifecycle remnants. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed; diff check only printed existing line-ending warnings.
- Added `WorldSacredPickupState` for LDtk world sacred-pickup cutscene state, moving pending/active LorePopup items, active weapon pulse, active anvil tether, pickup zoom override, and Rustborn discovery flags out of `LdtkWorldScene` while preserving scene-owned pickup and dialogue policy. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed; diff check only printed existing line-ending warnings.
- Added `WorldPlayerProgressionState` for LDtk world gold and HealthShard bonus counters, removing those direct fields from `LdtkWorldScene` while preserving save payload assembly, HUD update timing, and respawn's existing gold behavior. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `WorldItemWorldEntryState` for LDtk world-to-Item-World entry state, moving the collapsing item, active deployment sequence, pending ghost-tunnel scheduling params, ItemTunnel flag, pre-tunnel return level, and world-visual release flag out of `LdtkWorldScene`. Later same-day refactors moved tunnel collision mutation and pending ghost scheduling into dedicated flow runtimes; the scene still owns Item World scene creation and anvil return orchestration. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `WorldItemDeploymentCollisionRuntime` for the LDtk world Item World deployment `RuntimeCollisionScope`, removing direct scope construction/storage from `LdtkWorldScene` while preserving scene-owned clear/restore timing, player `roomData` reassignment, tile rerender timing, and ghost stream orchestration. Also removed the dead scene-level `wallGate` field/import; WallGate ownership remains inside deployment sequence implementations. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `WorldWeatherRuntime` for LDtk world `Weather` entity parsing and `WeatherSystem` lifetime, removing the direct weather field and parse/update helpers from `LdtkWorldScene` while preserving scene-owned layer creation, level-load timing, and builder dynamic-collider policy. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `WorldTerrainPaletteRuntime` for LDtk world terrain palette filters and filter bounds pinning, removing direct terrain palette/rim filter fields and `applyTerrainFilterAreas()` from `LdtkWorldScene` while preserving CSV palette sourcing, renderer/procedural filter application, and builder rim-filter sharing. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `WorldUpdraftRuntime` for LDtk world updraft system lifetime and active-builder updraft channel composition, removing direct `UpdraftSystem` ownership plus helper methods from `LdtkWorldScene` while preserving `player.roomData`-based physics and builder-grid visual/force channels.
- Added `WorldVoidFogRuntime` for LDtk world render-only void fog particle lifetime, separating it from `WorldVoidRuntime`'s void fade/input-lock state machine and clearing fog graphics during Item World visual release.
- Added `WorldSpawnState` for LDtk world spawn-level fallback and saved-level validation, removing direct `playerSpawnLevelId` ownership and stale-save resolution helpers from `LdtkWorldScene` while preserving Debug-room gating and `loadLevel()` ownership in the scene.
- Added `WorldContainerPhysicsRuntime` for LDtk world throwable-container body physics and overlap resolution, moving body updates, environmental impact checks, thrown-container enemy hits, player/enemy/container collision, pre-update top-support checks, and container-fluid flush delegation out of `LdtkWorldScene` while preserving scene-owned paint/VFX/fluid callbacks.
- Added `WorldContainerDestructionRuntime` for LDtk world throwable-container destruction VFX/SFX, aligning world container destruction ownership with the existing Item World destruction runtime while keeping break-decision and fluid-paint policy outside the destruction bundle.
- Added `WorldContainerFluidRuntime` for LDtk world throwable-container fluid paint/effects and dirty refresh timing, replacing `WorldContainerFluidState` and removing container splash/contact helpers from `LdtkWorldScene`. The world path still refreshes the full collision grid and rerenders the tilemap after dirty fluid mutations. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed; diff check only printed existing line-ending warnings.
- Added `WorldSecretWallRuntime` for LDtk world `SecretWall` spawn replay and attack handling, moving already-unlocked wall collision/tile clearing, wall break analytics, item/passage rewards, and toasts out of `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed; diff check only printed existing line-ending warnings.
- Added `WorldCrackedFloorRuntime` for LDtk world `CrackedFloor` spawn and shatter policy, moving collision injection, persistent crack-key handling, sword/surge/dive shatter checks, feedback, toasts, and registry cleanup out of `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed; diff check only printed existing line-ending warnings.
- Added `WorldSpikeRuntime` for LDtk world spike spawning and player spike-contact punishment, moving LDtk spike entity spawning, IntGrid/entity spike checks, damage, feedback, rumble, last-safe teleport, and death fallback out of `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed; diff check only printed existing line-ending warnings.
- Added `WorldGrowingWallRuntime` for LDtk world `GrowingWall` spawn/update/shatter policy, moving collision injection, persistent `gwall_*` keys, pending slime handoff, surge/dive shatter checks, feedback, hit sparks, toasts, and registry cleanup out of `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed; diff check only printed existing line-ending warnings.
- Added `WorldBreakableRuntime` for LDtk world `Breakable` spawn/update/sword-hit destruction, moving authored sprite parsing, hit checks, shatter VFX/SFX, hit sparks, gold/flask drops, and registry cleanup out of `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed; diff check only printed existing line-ending warnings.
- Added `WorldBreakablePropRuntime` for LDtk world procedural `BreakableProp` placement and destruction policy, moving spawn exclusions, `TileMutator` registration, sword/fire cleanup, shatter VFX/SFX, hit sparks, gold/flask drops, and pickup registration out of `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed; diff check only printed existing line-ending warnings.
- Added `WorldCollapsingPlatformRuntime` for LDtk world `CollapsingPlatform` spawn and collapse-policy ownership, moving host LDtk spawn, player-on-top updates, state-change builder stamp refresh, and permanent-collapse persistence out of `LdtkWorldScene`. Builder-spawned platform creation remains in builder dispatch for attachment wiring but shares registry metadata. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed; diff check only printed existing line-ending warnings.
- Added `WorldBuildingRuntime` for LDtk world `Building` visual spawn ownership, moving tile-picker validation and active-level visual construction out of `LdtkWorldScene` while keeping `Building` collision-free. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed; diff check only printed existing line-ending warnings.
- Added `WorldBurnablePropRuntime` for LDtk world Tier B `BurnableProp` spawn and burned-out cleanup policy, moving `BurnableZone` parsing, `TileMutator` burnable registration/unregistration, ash remnant spawning, and burnable-prop update/removal out of `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed; diff check only printed existing line-ending warnings.
- Added `WorldDoorSwitchSpawnRuntime` for host LDtk `LockedDoor` / `Switch` spawning, moving active-level field parsing, collision injection, persisted unlock activation, and registry registration out of `LdtkWorldScene`. Attack/unlock feedback and builder-spawned door/switch construction remain scene-owned. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed; diff check only printed existing line-ending warnings.
- Added `WorldDoorSwitchInteractionRuntime` for LDtk world `LockedDoor` / `Switch` interaction policy, moving attack blocking, collision maintenance, player-door overlap resolution, door animation updates, event/IID unlocks, stat-gate feedback, switch attacks, analytics, rumble, camera shake, screen flash, toasts, and builder-stamp refresh callbacks out of `LdtkWorldScene`. Builder-spawned construction remains in builder dispatch. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldEdgeTransitionFlowRuntime` for LDtk world passable-edge transition flow, moving edge detection, neighbor selection trigger, item-world corridor fallback, tunnel-exit transition start, and transition fade update delegation out of `LdtkWorldScene`. Fade state remains in `WorldEdgeTransitionRuntime`; stateless geometry remains in `WorldTransitionController`; scene still owns actual `loadLevel()` and `ItemWorldScene` creation. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `WorldAnvilRetirementRuntime` for LDtk world post-boss anvil retirement policy, moving `RetireAfterFirstBoss` parsing, spawn-disabled decisions, boss-clear retirement checks, return-event marking, prompt hiding, anvil inventory close, and preserved-item clearing out of `LdtkWorldScene`. Scene still owns anvil spawn/update, placement, reclaim, and deployment orchestration. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `WorldAnvilInteractionRuntime` for LDtk world anvil per-frame interaction, moving player-near-anvil checks, prompt suppression/visibility policy, retired-anvil disabled prompt display, deployment-time prompt hiding, anvil visual update, and placed-item strike detection out of `LdtkWorldScene`. Scene still owns anvil spawn, placement/reclaim, inventory opening, and floor-collapse deployment construction. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `WorldAnvilSpawnRuntime` for LDtk world host `Anvil` spawning, moving host-level anvil cleanup, prompt reset, `RetireAfterFirstBoss` initial disabled handling, entity-layer attachment, and Altar fallback out of `LdtkWorldScene`. Later same-day work extended it to own builder-mounted anvil creation and attachment. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `WorldAnvilItemRuntime` for LDtk world anvil item placement/reclaim UI actions, moving placed-item reclaim, inventory-full return handling, prompt suppression, anvil inventory open, first-dive marking, item state handoff, and floor-collapse start callback out of `LdtkWorldScene`. Scene still owns floor-collapse deployment construction. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `WorldAnvilDeploymentRuntime` for LDtk world anvil floor-collapse deployment start orchestration, moving prompt/savepoint/UI hiding, anvil used state, return-state recording, pre-tunnel level handoff, dive counter increment, initial hitstop/camera/strike feedback, `ItemWorldEntrySequence` creation, active deployment storage, and sequence start out of `LdtkWorldScene`. Later same-day refactors moved lower-level tunnel collision, ghost-stream restore ordering, and laser/atmosphere sequencing into dedicated flow runtimes; the scene still owns Item World scene callbacks. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `WorldItemDeploymentTunnelFlowRuntime` for LDtk world Item World deployment tunnel flow, moving tunnel opening, ghost overlay teardown, ghost/world collision restore ordering, player `roomData` synchronization, and deployment tunnel visual restore out of `LdtkWorldScene`. Scene still owns higher-level Item World entry/return and actual `ItemWorldScene` creation. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldItemDeploymentAtmosphereFlowRuntime` for LDtk world Item World deployment atmosphere flow, moving laser desaturation orchestration, dungeon-atmosphere activation/deactivation side effects, frozen snapshot interaction cleanup, player `entityLayer`/`vividLayer` reparenting, and pending ghost-tunnel scheduling after laser release out of `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `WorldItemWorldSceneFlowRuntime` for common LDtk world-to-`ItemWorldScene` flow, moving entry prestream delegation, ItemWorldScene construction/tutorial hydration, prepared-scene push prep, scene pop/return fade/stat refresh/tutorial flag/inventory hint, and earned-gold callback out of `LdtkWorldScene`. Later same-day refactor moved fixed-level enter/exit policy into `WorldFixedItemWorldFlowRuntime`; portal/altar/anvil paths still own path-specific reward, dialogue, and anvil retirement policy. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldFixedItemWorldFlowRuntime` for LDtk world fixed Item World enter/exit policy, moving fixed-level validation/load, missing-level procedural fallback, fixed exit return-level resolution, entry item cleanup, player anvil-return placement callback, inventory hint, return dialogue, and anvil retirement sequencing out of `LdtkWorldScene`. Fixed boss kill reward/progression while inside the authored fixed level still remains scene-owned. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldPortalItemWorldFlowRuntime` for LDtk world portal/altar Item World completion policy, moving pending portal consumption, fixed-world portal exit routing, portal-created dungeon item generation, common ItemWorldScene flow delegation, return dialogue/anvil retirement, acquisition/level-up/ATK toasts, and sacred pickup handoff out of `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldAnvilItemWorldFlowRuntime` for LDtk world anvil/tunnel Item World entry completion policy, moving direct growth handoff, archived rarity-mapped ItemTunnel loading, fixed item override routing, procedural ItemWorldScene creation delegation, and anvil-path level-up/ATK/dialogue/retirement side effects out of `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldAnvilReturnFlowRuntime` for LDtk world Item World anvil-return restoration, moving return-level reload, edge/tunnel/entry-state cleanup, preserved placed-item restoration after `loadLevel()`, and player placement at the used-anvil snapshot out of `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldPlayerSpawnRuntime` for LDtk world player placement on level load, moving edge-entry spawn selection, LDtk Player entity fallback, runtime-collision-grid placement, player `roomData` assignment, velocity reset, previous-position save, and void safe-position recording out of `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldContainerSpawnRuntime` for LDtk world initial container spawning, moving authored `Container` parsing, initial `ContainerSpawner` execution, debug spawner rectangles, stable per-level auto-seeding, maintained-spawner registration, and spawn-time settling out of `LdtkWorldScene.loadLevel()`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extended `WorldFluidRuntime` to own level attach and LDtk `FluidSpawner` parsing via `attachLevel(level)`, removing direct fluid-spawner entity loops from `LdtkWorldScene.loadLevel()` while preserving per-frame fluid update order in the scene. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extended `WorldEndingRuntime` to own LDtk `EndingTrigger` parsing via `loadLevel(level)`, removing ending-trigger handling from `LdtkWorldScene.processLdtkEntities()` and narrowing the runtime's public API to level load/update/check/destroy. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extended `SavePointRuntime`, `WorldPickupRuntime`, and `WorldRelicPickupRuntime` to own LDtk `GameSaver`, `GoldPickup`, `HealingPickup`, `HealthShard`, and `AbilityRelic` parsing during level load, removing those cases from `LdtkWorldScene.processLdtkEntities()` while preserving save payload and progression side effects in scene callbacks. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extended `CameraZoneRuntime` to own host-world LDtk `Camera` parsing and added `WorldHandPlacedItemRuntime` for host-level LDtk `Item` parsing/telemetry, removing `LdtkWorldScene.processLdtkEntities()` entirely. Fixed item creation policy moved to `WorldFixedItemSpawnRuntime` later in the same session. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldFixedItemSpawnRuntime` for fixed item creation policy, moving Content_Item_Master lookup, direct weapon fallback, weapon `ItemDropEntity` creation, currency `GoldPickup` creation, and consumable toasts out of `LdtkWorldScene.spawnFixedItemAt()`. Hand-placed items, secret-wall rewards, and builder-mounted item entities now share this runtime. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldBuilderItemRuntime` for Giant Builder-mounted LDtk `Item` parsing and attachment, removing the builder `case 'Item'` block from `LdtkWorldScene.spawnBuilderEntities()` while preserving collected-key behavior, `sword_broken` skip/mark behavior, fixed-item spawn delegation, and item-drop/gold/healing attachment liveness checks. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldBuilderStaticEntityRuntime` for Giant Builder-mounted `Spike`, `Breakable`, and `CollapsingPlatform` creation/attachment, removing those builder cases from `LdtkWorldScene.spawnBuilderEntities()` while preserving registry ownership, breakable sprite fallback, collapsing-platform persistence keys, builder collision injection, and attachment liveness checks. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldBuilderDoorSwitchRuntime` for Giant Builder-mounted `LockedDoor` and `Switch` creation/attachment, removing those builder cases from `LdtkWorldScene.spawnBuilderEntities()` while preserving door field parsing, switch target parsing, builder-grid collision injection/activation, persisted unlock handling, registry ownership, and attachment liveness checks. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldBuilderEntranceRuntime` for Giant Builder-mounted `BuilderEntrance` / `BuilderEntity` glow attachment, removing that builder case from `LdtkWorldScene.spawnBuilderEntities()` while preserving `WorldExitGlowRuntime` spec parsing, builder-offset anchor sync, builder entrance glow lifetime checks, and non-reparented world-space glow containers. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extended `WorldAnvilSpawnRuntime` with builder-mounted `Anvil` spawning, removing the final direct gameplay construction case from `LdtkWorldScene.spawnBuilderEntities()` while preserving single-Anvil policy, `RetireAfterFirstBoss` parsing, initial disabled state, and builder attachment liveness. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extended `WorldBuilderSpriteRuntime` with `spawnIfSprite()`, removing the last `switch` block from `LdtkWorldScene.spawnBuilderEntities()` so builder entities are dispatched through focused runtimes instead of scene-local cases. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extended `InventoryTutorialHintRuntime` to read first Item World return hint retire/blocking conditions through callbacks and removed scene-local inventory hint and anvil dive UI wrapper methods from `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldSacredPickupRuntime` for LDtk world sacred-pickup cutscene policy, moving first-pickup/first-seen pulse selection, Rustborn discovery, LorePopup/DivePreview input, AcquireOverlay update delegation, anvil tether sync, inventory-hint blocking predicates, and Ego wake/discovery event dispatch out of `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extended `WorldRelicPickupRuntime` to own HealthShard and AbilityRelic reward side effects, removing scene-local relic reward handlers while preserving collectedRelics mutation, HealthShard bonus/full heal, ability flags, acquire overlays, analytics, hitstop, screen flash, and camera shake through callbacks/runtime getters. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extended `WorldProjectileRuntime` to own hostile projectile deflect and player-hit side effects, removing scene-local projectile callbacks while preserving hit sparks, player damage/knockback/invincibility, HUD flash, hitstop, camera shake, screen flash, damage numbers, and death handling. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extended `WorldMovementVfxRuntime` to own LDtk world player kinematic VFX event consumption/spawn timing, moving landing/dash/jump/wall-slide/footstep/surge/dive-impact VFX and footstep/landing SFX out of `LdtkWorldScene` while leaving fluid, residue, enemy VFX, hit blood, and drop-through tutorial hooks scene-owned. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldFluidFeedbackRuntime` for LDtk world fluid/residue feedback during movement VFX, moving player water/non-water transitions, residue timers/hazard ticks, bubbles, enemy fluid transitions, enemy movement VFX, and enemy residue damage out of `LdtkWorldScene` while preserving drop-through tutorial hooks, player ice-skid emission, player hit blood, and update ordering in the scene. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldSaveRuntime` for LDtk world save execution and payload assembly, moving full heal/flask refill on save, save flash/hitstop/pulse ordering, `SaveManager.save()` parameters, saved toast, `trackSave()`, and HUD refresh out of `LdtkWorldScene.performSave()`. `SavePointRuntime` still owns proximity and delayed save queue timing. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extended `WorldEnemyRegistry` with `hasAliveWithin()` and removed the scene-local `hasEnemyNearby()` loop from `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldPlayerStatRuntime` for LDtk world player stat synchronization, moving ATK/DEF/MaxHP recalculation, innocent bonuses, cheat bonus, equipped weapon metadata, and attack hitbox multiplier updates out of `LdtkWorldScene.updatePlayerAtk()`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldEnemyKillRuntime` for LDtk world enemy kill side effects, moving kill stats/analytics, target-door unlocks, boss key marking, boss flash/lock cleanup, fixed Item World boss item level-up/portal scheduling, gold drops, and healing drops out of `LdtkWorldScene.handleEnemyKill()`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extended `WorldContainerSpawnRuntime` with `debugSpawnNear()` and removed the scene-local Shift+G `debugSpawnContainers()` helper. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldEgoDialogueRuntime` to move remaining world Ego dialogue sequencing out of `LdtkWorldScene`:
  - anvil-proximity `EGO_ANVIL` hint check in `WorldEgoDialogueRuntime.update()`
  - world-return/retirement cue selection in `fireWorldReturnDialogue()`
  - equipped-weapon swap cue in `notifyWeaponSwap()`
  - restored first-boss retirement gating parity through `sacredSave.isFirstItemWorldBossDefeated()` and active-lore-queue guards.
  - Verification: `npm run build`, `npm run build:offline:ko`.
