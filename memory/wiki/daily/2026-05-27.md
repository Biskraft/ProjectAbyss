---
date: 2026-05-27
sessions: 1
---
# 2026-05-27 Work Log

## Session 1 - iPad startup memory reduction

- Capped `Game.uiScale` at 2 for the touch-Apple reduced visual profile to avoid 3x iPad native render targets.
- Deferred `item_world` bundle preload on that profile; `ItemWorldScene.init()` now awaits the bundle so loading happens during the black entry fade instead of at startup.
- Verification: `npx tsc --noEmit` and `npm run build` from `game/` pass. Remaining warnings are existing LDtk/CSV tileset divergence warnings plus the existing Vite large chunk warning.
