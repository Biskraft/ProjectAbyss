# 2026-06-13

## 아이템계 스폰 2축 모델 가동 — 권속 그리드 v2 + M1-B 구현

- **로스터 전수 재확인:** 구현 31종(전투 29 + Guardian + GoldenMonster), EnemyFactory·Content_Enemy.csv·Content_Stats_Enemy.csv 3중 일치. §6.1의 "필수 신규 Spitter"는 B07_Gunner가 거동으로 충족(B07×rust=Spitter).
- **RES-IWS-01 §6.4 신설(캐논):** 권속 그리드 v2(31종 시그니처 배치) + 조성 레시피 5종(forge 폭탄 식별 / iron 진지전 / rust 소모전 / spark 시선 관리 / shadow 3방향 기습+가시 앵커) + 혼합 권속 규칙(shadow×forge 최고 시너지, Carrier+AirBrood 식별 쌍, R11+함정 과밀 금지).
- **Content_ItemWorld_SpawnTable.csv 재작성:** 구 `Rarity,Stratum,EnemyType,...` 폐기 → §7.1 계약 스키마 `Family,Behavior,FluidOverride,MinStratum,MaxStratum,Weight,ClusterMin,ClusterMax`. 5권속 풀 + boss 풀, 24종 투입. **보류 2종:** B50·B52(surface — 절차 방 천장/벽 앵커 검증 전, 월드 전용).
- **itemWorldSpawnTable.ts 파서 재작성:** getFamilyPool(family, stratum)/getBossEntry — 깊이 창 필터, temperamentPrimary→권속 키.
- **ItemWorldEnemyEncounterRuntime.spawnForRoom 교체(M1-B):** 단일 종 1픽 폐기 → 역할 예산 채우기 루프. 예산=BaseEnemyCount+EnemyCountBonus(소비 연결 완료), 상수 비율 45/30/20/5, 리테넌트 캡 1, **대응 동사 2종 보증 가드**(예산 막바지 distinct Response<2면 다른 R 우선).
- **Content_Enemy.csv += Response 칼럼**(R1~R6·R11, 변별감사 SSoT 동기화. Guardian/GoldenMonster 공란). enemyStats.ts에 Role·Response 파싱 + getEnemyRole/getEnemyResponse 접근자.
- **보류(M2):** FluidOverride 런타임 적용(Enemy에 attribute 필드 부재 — fluid 모듈 부착 별도), Content_RoleComposition.csv(무기 종류 편향), 부색 혼입 20~30%.
- 검증: tsc 통과 + Sheets validate.mjs OK(기존 경고 1건). 인게임 체감(M1 Exit Criteria — 방당 2기능군 시드 무관, rust 소모전 가독)은 플레이 검증 대기.

## 테스트 전략 + LDtk 레시피 방 작업서

- 4층 검증 전략 수립: ①validate.mjs SpawnTable 정합 확장(존재성·커버리지) ②시드 전수 시뮬레이션 ③인게임 조성 로그+기질별 다이브 ④체감·성능(M1 Exit). LDtk 손배치=레시피 체감 / 아이템계 다이브=시스템 검증으로 **판정 분리** 원칙 확정.
## 권속 테스트용 매직 sword 5종

- `Content_Stats_Weapon_List.csv` += 5행: sword_test_{forge/iron/rust/spark/shadow}. **5종 스탯 동일**(sword_magic 기준 BaseATK69·1.0·70/52/22·two_arc_pocketed) — temperamentPrimary + ThemeID만 권속별 차등(권속 변수 격리 목적). Theme: forge=T-FOUNDRY, iron=T-COOLANT, rust=T-MALFUNCTION, spark=T-COMMAND, shadow=T-ECHO.
- Content_Localization.csv += 이름 5키(en/ko, "Test Blade - 권속"). ※ em대시(—) validate V4 금지 → ASCII 하이픈으로 교정(게이트가 잡음).
- **드랍 풀 오염 방지:** weapons.ts에 `TEST_ONLY_IDS` 신설(STARTER_ONLY_IDS와 별개 — **다이브 차단 안 함**, 드랍에서만 제외). ItemRewardFactory 3함수 모두 `isDroppable()` 가드로 교체(특히 createRandomRareOrBetterRewardItem의 filter가 기존엔 STARTER도 미제외였음 → 동시 보강).
- **Item_Master 등록:** Content_Item_Master.csv += 5행(category=weapon, rarity=magic, SourceKey=무기ID, IdentityCategory=Tutorial — 비진화 단일 이름). DescKey용 `.desc` 5키도 로컬라이제이션 추가. ALL_ITEM_IDS 등재로 getMasterItem 정상 해석.
- 획득: LDtk `ItemSpawner`(ItemId=무기ID) → WorldFixedItemSpawnRuntime 드랍. ExternalEnums 재생성으로 ItemId enum 38→48(Item_Master 소스), 에디터 드롭다운 노출 확인.
- 검증: csv_to_locale 706키 0 missing + validate OK + tsc EXIT=0.

- `Task_LDtk_FamilyRecipeRooms.md` 신설 — 레시피 방 5종 배치 명세. 기존 스트립 실측 규격(Overworld_Level_63~90, 768×384px 48×24셀 grid16, Enemy_Spawn type/level=1, RoomType=Debug) 준수. 방별 셀 좌표 + detect 기반 간격 가이드(R11 인터리브 필수, 후방 화력 detect 직전, 공중 수직 6셀 분리) + 방별 체크 질문 + 한계 명시(fluid 컨텍스트 없음 → rust 체감은 다이브 이월).
