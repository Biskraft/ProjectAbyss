# 2026-06-05

- ItemWorld/World scene 라인에서 패스스루 메서드 재스캔을 수행했으며, Scene 레이어에서 즉시 제거 가능한 단일 위임형 메서드는 미확인.
- `WorldEndingRuntime`의 단일 위임 함수(`addTrigger`)를 제거해 트리거 적재 경로를 직접 배열 push로 정리.
- `track`/`enter/exit` 경로 안정성 유지가 최우선인 상태에서 다음 큐는 포털/복귀/트랩도어 전이 가드 정합성 검증으로 이동.
- `WorldItemWorldSceneTransitionRuntime.startReturnFade`를 필수 의존성으로 정리했다. LDtk는 `ItemWorldReturnFadeRuntime.start()`를 주입하고, legacy `WorldScene`은 return fade가 없는 경계임을 no-op으로 명시한다.
- `ItemWorldTrapdoorRuntime`이 입력 처리 중 `trapdoor.activate()`를 먼저 호출해 flow guard가 `consumed` 상태에서 하강을 막을 수 있던 순서를 정리했다. 활성화/소비는 `ItemWorldTrapdoorFlowRuntime.startDescent()` 내부의 가드와 스냅샷 캡처 이후에 수행한다.
- 컴파일 표면 정리: `ItemWorldScene` 중복 `wireStratumRuntimes` 이름을 분리하고, Breakable feedback helper texture 타입(`Texture | null`), HUD ghost-bar 상수 import, Inventory visibility/facade/anvil-placement 타입 계약을 현재 코드와 맞췄다.
- 검증: `game/`에서 `npx tsc --noEmit` 통과, `npm run build` 통과. 빌드는 localization 692 keys OK, CSV validate OK, 기존 `atlas/prologue_01.png` LDtk/CSV warning 1건을 출력했다.
- World/ItemWorld Ego Shard impact 동기화: 공통 24px fire footprint 수집 로직을 `EgoShardImpactHelpers.getEgoShardFireImpactFootprint()`로 이동하고, world/itemworld 런타임은 모드별 tile side effect만 유지한다.
- World/ItemWorld Ego Shard projectile wrapper 동기화: `EgoShardProjectileHelpers.updateEgoShardProjectileRuntime(deps, dtMs)`를 추가해 두 wrapper의 동일한 deps 매핑 반복을 제거했다.
- World/ItemWorld hostile projectile 피격 feedback 동기화: `ProjectileCollisionHelpers.applyProjectilePlayerHitFeedback()`로 플레이어 피격/flash/damage-number/death 처리를 공유하고, world 런타임의 중복 damage number spawn drift를 제거했다.
- World/ItemWorld pickup collection 호출부에서 기본 proximity 콜백 재래핑을 제거해 `PickupCollectionHelpers.processPickupsForPlayerCollection()`의 기본 player proximity 경로를 직접 사용하도록 정리했다.
- World/ItemWorld weather collision solid predicate를 `WeatherRuntimeHelpers.isWeatherCollisionSolid()`로 공유해 rain/snow/stratum weather가 동일하게 solid + one-way tiles를 충돌로 본다.
- Item World LDtk Player spawn의 bottom-center 좌표를 runtime top-left로 변환하는 순수 좌표 계산을 `PlayerPlacementHelpers.playerTopLeftFromBottomCenter()`로 이동했다. World spawn 정책은 edge/default semantics가 달라 그대로 유지.
- World/ItemWorld enemy spawn 런타임이 `EnemySpawnInitializationDeps` adapter를 생성자에서 한 번 구성해 `initializeEnemySpawnedEntity()` 호출에 재사용하도록 정리했다.

- ContainerPhysics world/itemworld update orchestration now routes through game/src/scenes/shared/ContainerPhysicsRuntimeHelpers.ts; mode runtimes keep only dependency wiring and mode-specific public surface.

- ContainerFluid impact side effects now share pplyContainerFluidImpactSideEffects() in game/src/scenes/shared/ContainerFluidHelpers.ts; world/itemworld keep distinct splash-paint and refresh policies.

- ContainerCarry world/itemworld grab, held-carry, and tether state advancement now share game/src/scenes/shared/ContainerCarryStateHelpers.ts; prompt ownership and ArcTether attachment remain mode-owned.

- BreakableProp post-break feedback and drop consequences now share game/src/scenes/shared/BreakablePropDestructionHelpers.ts; reak(), registry removal, and final destroy ownership remain mode-specific.

- Enemy world/itemworld drop orchestration now shares spawnEnemyDrops() in game/src/scenes/shared/EnemyCombatDropHelpers.ts; each runtime still passes explicit coordinate policy and gold/healing order.

- TileHazard player/enemy damage feedback and waterfall hazard ticks now share game/src/scenes/shared/TileHazardRuntimeHelpers.ts; world/itemworld keep distinct grid scope, fluid simulation ordering, and active-bounds policies.

- EgoShardCombat world/itemworld getter dependency adaptation now shares checkEgoShardCombatRuntimeHit() in game/src/scenes/shared/EgoShardCombatHelpers.ts; mode runtimes remain wiring shells.

- EgoShardCast world/itemworld input/getter adaptation now shares updateEgoShardCastRuntime() in game/src/scenes/shared/EgoShardCastHelpers.ts; Item World keeps its reset bridge.

- EnemyRegistry world/itemworld simple query/update/render loops now share game/src/scenes/shared/EnemyRegistryHelpers.ts; mode registries keep only public API wrappers and ownership boundaries.

- ItemWorld Ego dialogue active/flagged/idle LoreDisplay fire gate now shares game/src/scenes/shared/EgoDialogueRuntimeHelpers.ts; world return queue and weapon-swap event semantics remain mode-specific.

- ItemWorld HUD cleared-strata calculation now shares game/src/scenes/shared/ItemWorldHudHelpers.ts; ItemWorldHudRuntime remains the HUD presentation adapter.

- ItemWorld static-entity destroy-and-clear list lifecycle now shares game/src/scenes/shared/StaticEntityRegistryHelpers.ts; static spawn/runtime interaction policies remain mode-specific.

- Legacy WorldScene inventory equip HUD count now uses ui.hud.items_count instead of hardcoded Items:{count}, preserving Localization CSV SSoT.

- 2026-06-05: Combat milestone localization cleanup: world and Item World 100-damage special number calls now use ui.combat.milestone_100_damage from Sheets/Content_Localization.csv. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- World/ItemWorld hostile projectile update/remove loop now shares updateProjectileCollection() in game/src/scenes/shared/ProjectileCollectionHelpers.ts; deflect/player-hit policies remain mode-owned. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- ItemWorld anvil/resident list lifecycle now shares game/src/scenes/shared/EntityLifecycleHelpers.ts for add/update/destroy-clear repetition. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- World static registry lifecycle repetition now shares EntityLifecycleHelpers optional attach/destroy-clear/remove-at helpers across Building, CrackedFloor, GrowingWall, Spike, and SecretWall registries. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- Additional world registry lifecycle cleanup: Breakable, CollapsingPlatform, DoorSwitch, and BreakableProp helper paths now reuse EntityLifecycleHelpers while preserving registry-specific metadata. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- World item-drop, altar, and portal lifecycle cleanup now shares EntityLifecycleHelpers; altar title uses existing ui.world.offer_item localization key. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- WorldExitGlowRuntime glow lifecycle now shares EntityLifecycleHelpers for add/clear/remove while preserving builder subset fallback destroy behavior. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- WorldRelicPickupRuntime health-shard lifecycle now shares EntityLifecycleHelpers while ability relic Graphics cleanup remains runtime-specific. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- ItemWorldStaticEntitySpawner push/addChild repetition now shares EntityLifecycleHelpers.addEntityToLayer while retaining spawn policy and collision injection locally. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- Debug/runtime container spawn attach paths now share EntityLifecycleHelpers.addEntityToLayer across ItemWorld debug spawns, ItemWorld runtime cells, and world maintained refills. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- Boss-lock door attach and Item World growing-wall slime attach now share EntityLifecycleHelpers.addEntityToLayer while preserving unlock/destroy and slime initialization order. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- WorldDialogueTriggerRuntime NPC add/update/destroy-clear lifecycle now shares EntityLifecycleHelpers while trigger prompt cleanup and dialogue state remain runtime-owned. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- World/ItemWorld weather container attachment now shares WeatherRuntimeHelpers.attachWeatherToLayer while mode-specific weather construction remains local. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- World/ItemWorld EgoShardProjectile runtime wrappers now share EgoShardProjectileRuntimeAdapter while preserving mode-specific class names. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- World/ItemWorld EgoShardCombat runtime wrappers now share EgoShardCombatRuntimeAdapter while preserving mode-specific class names. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- World/ItemWorld EgoShardCast update wrappers now share EgoShardCastRuntimeAdapter while Item World keeps its reset bridge. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- World/ItemWorld EgoShardImpact debug freeze/thunder traversal now shares EgoShardImpactHelpers while runtime-specific logging/toast policy remains local. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- World/ItemWorld EgoShardImpact debug fire traversal/actions now share EgoShardImpactHelpers while runtime-specific logging/toast policy remains local. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- World/ItemWorld EgoShardImpact ice impact handling now shares EgoShardImpactHelpers.applyEgoShardIceImpact while mode-specific fire/thunder behavior remains local. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- BurnablePropRegistryHelpers now reuses EntityLifecycleHelpers for add/clear/remove lifecycle while TileMutator and ash policies remain runtime-owned. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- Shared CellExclusionHelpers.addCellExclusionRadius() for World/ItemWorld breakable prop spawn exclusion sets; mode-specific exclusion anchors remain runtime-owned. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- Added DisplayObjectLifecycleHelpers for Pixi display-object detach/destroy/splice and applied it to ItemWorld capture orbs plus World ability relic markers. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- ItemWorldMemoryTriggerRuntime now uses DisplayObjectLifecycleHelpers for particle expiry and trigger visual cleanup, including explicit Graphics destroy on clear. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- Extended DisplayObjectLifecycleHelpers with Pixi destroy options and applied it to ItemWorldDevOverlayRuntime plus ItemWorldContainerPromptRuntime owned display cleanup. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- Extended PickupCollectionHelpers with point-based player proximity and applied shared proximity checks to WorldRelicPickupRuntime health shards and ability relic markers. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- ItemWorldEntryCorridorVisualRuntime now uses DisplayObjectLifecycleHelpers for root container cleanup with child destruction preserved. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- Added DisplayObjectLifecycleHelpers.detachDisplayObject() and applied minimap detach/destroy cleanup in WorldMinimapRuntime while preserving non-destructive detach behavior. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- Routed additional owned display cleanup through DisplayObjectLifecycleHelpers in WorldGameOverRuntime, ItemWorldFullMapLayerRuntime, and ItemWorldPrologueEndRuntime while preserving destroy vs detach-only semantics. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- WorldAltarController now centralizes altar UI teardown and uses DisplayObjectLifecycleHelpers for owned selection panel cleanup. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- Applied DisplayObjectLifecycleHelpers to ItemWorldCellVisualRuntime per-cell layer teardown and ItemWorldStratumPickerRuntime picker root cleanup, preserving destroy options. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- WorldDialogueTriggerRuntime now uses DisplayObjectLifecycleHelpers.detachDisplayObject() for interact prompt cleanup while preserving detach-only prompt semantics. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

- WorldUiController now uses detachDisplayObject() for external UI container detach/teardown while preserving non-destroy ownership semantics. Verified with npx tsc --noEmit and npm run build from game/; build still reports the existing atlas/prologue_01 CSV warning only.

## ItemWorldUiController lifecycle cleanup

- Routed `game/src/scenes/itemworld/ItemWorldUiController.ts` escape confirm, boss choice, onboarding, return result, and stratum-clear overlay cleanup through `DisplayObjectLifecycleHelpers`.
- Preserved existing ownership: detach-only panels remain detach-only, owned Pixi containers still destroy children, and `StratumClearOverlay.destroy()` still owns overlay teardown after detach.
- Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


## ItemWorldStratumPickerRuntime hide cleanup

- Routed `game/src/scenes/itemworld/ItemWorldStratumPickerRuntime.ts` `hide()` through the existing `hideContainerOnly()` helper so dismissal and redraw replacement share the same `DisplayObjectLifecycleHelpers` teardown path.
- Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


## Prompt runtime display cleanup

- Routed owned prompt/modal teardown through `DisplayObjectLifecycleHelpers.destroyDisplayObject(..., { children: true })` in `game/src/scenes/world/AnvilCyclePromptRuntime.ts`, `game/src/scenes/world/AnvilPromptController.ts`, and `game/src/scenes/itemworld/ItemWorldWorldPromptRuntime.ts`.
- Preserved existing ownership: these runtimes still own prompt creation/replacement/destroy; callers still own interaction policy and suppression.
- Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


## Transition overlay display cleanup

- Routed overlay detach/destroy through `DisplayObjectLifecycleHelpers` in `game/src/scenes/world/WorldAcquireOverlayRuntime.ts`, `game/src/scenes/world/ItemWorldEntryPushTransition.ts`, and `game/src/scenes/world/ItemWorldReturnFadeRuntime.ts`.
- Preserved existing semantics: acquire overlay reparent uses detach-only, acquire destroy owns children, and transition fade Graphics are destroyed after their existing try/finally or runtime destroy paths.
- Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


## Frozen return display cleanup

- Routed runtime-owned prompt, confirm panel, white return overlay, and frozen snapshot container teardown through `DisplayObjectLifecycleHelpers` in `game/src/scenes/world/WorldFrozenReturnRuntime.ts` and `game/src/scenes/world/WorldFrozenSnapshotRuntime.ts`.
- Preserved existing runtime ownership: proximity unregister and ticker removal still happen before display teardown, and snapshot filter references are still cleared after container destroy.
- Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


## Save/debug/intro display cleanup

- Routed save point marker/sprite/prompt detach through `DisplayObjectLifecycleHelpers.detachDisplayObject()` in `game/src/scenes/world/SavePointRuntime.ts`.
- Routed debug warp hint and title handoff overlay teardown through `DisplayObjectLifecycleHelpers.destroyDisplayObject()` in `game/src/scenes/world/WorldDebugWarpRuntime.ts` and `game/src/scenes/world/WorldIntroHandoffRuntime.ts`.
- Preserved existing semantics: save point objects are detached only, while debug hint and title overlay are destroyed by their owning runtimes.
- Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


## Builder/growth display cleanup

- Routed builder-mounted entity reparent detach through `DisplayObjectLifecycleHelpers.detachDisplayObject()` in `game/src/scenes/world/WorldBuilderAttachmentRuntime.ts`.
- Routed builder auxiliary layer teardown through `DisplayObjectLifecycleHelpers.destroyDisplayObject(..., { children: true })` in `game/src/scenes/world/WorldBuilderLayerRuntime.ts`.
- Routed Item World growth snapshot vignette/container cleanup through `DisplayObjectLifecycleHelpers.destroyDisplayObject()` in `game/src/scenes/world/ItemWorldGrowthSnapshotController.ts`.
- Preserved existing semantics: reparent remains detach-only, owned layers and snapshot containers still destroy children, and texture/filter/hidden-source restoration stays controller-owned.
- Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


## Shared detach helper cleanup

- Routed enemy container remove/clear detach through `DisplayObjectLifecycleHelpers.detachDisplayObject()` in `game/src/scenes/shared/EnemyRegistryHelpers.ts`.
- Routed procedural decor layer detach through `DisplayObjectLifecycleHelpers.detachDisplayObject()` in `game/src/scenes/shared/ProceduralDecorLayerHelpers.ts`.
- Preserved existing semantics: these helpers detach containers/layers only and do not destroy enemies, decorators, or generated children.
- Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


## Title/ending scene UI root cleanup

- Routed standalone UI root teardown through `DisplayObjectLifecycleHelpers.destroyDisplayObject(..., { children: true })` in `game/src/scenes/TitleScene.ts` and `game/src/scenes/EndingScene.ts`.
- Preserved existing ownership: both scenes still own their UI root containers, TitleScene still removes the gamepad listener before teardown, and EndingScene CTA input/navigation behavior is unchanged.
- Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


## Reusable UI component teardown cleanup

- Routed owned root-container teardown through `DisplayObjectLifecycleHelpers.destroyDisplayObject(..., { children: true })` in `game/src/ui/AcquireOverlay.ts`, `game/src/ui/DivePreview.ts`, `game/src/ui/LorePopup.ts`, `game/src/ui/ItemImage.ts`, `game/src/ui/WorldMapOverlay.ts`, and `game/src/ui/FpsCounter.ts`.
- Preserved existing semantics: component containers remain owned by each UI class, and internal redraw/child replacement paths were not changed.
- Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


## Additional UI component display cleanup

- Routed owned root/Graphics teardown through `DisplayObjectLifecycleHelpers.destroyDisplayObject()` in `game/src/ui/AreaTitle.ts`, `game/src/ui/StratumClearOverlay.ts`, and `game/src/ui/OxygenOverlay.ts`.
- Routed `TutorialHint` panel/container removal through `DisplayObjectLifecycleHelpers.detachDisplayObject()` in `game/src/ui/TutorialHint.ts`, preserving its prior detach-only behavior.
- Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


- Pause/save transient UI cleanup: routed pause-menu submodal panels and save delete-confirm panel through `DisplayObjectLifecycleHelpers.destroyDisplayObject(..., { children: true })`; routed toast and damage-number transient detach paths through `detachDisplayObject()` while preserving damage-number manager clear-time destroy. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


- HUD/inventory redraw display cleanup: routed HUD skin reparent/depth-label/flask-icon detach paths through `detachDisplayObject()` and Inventory divider/frame/title redraw replacements through `destroyDisplayObject()`, preserving prior detach vs destroy ownership. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


- Scene-level display cleanup: routed legacy `WorldScene`, `ItemWorldScene`, and `LdtkWorldScene` direct Pixi remove/destroy paths through `DisplayObjectLifecycleHelpers`, preserving detach-only shared UI/marker/enemy semantics and owned destroy semantics for altar UI/collision debug HUD. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


- Effect root-container display cleanup: routed owned root teardown in `AnvilTether`, `ArcTether`, `PortalRingEffect`, `TransitionOverlay`, `PortalTransition`, and `WeaponPulse` through `DisplayObjectLifecycleHelpers.destroyDisplayObject(..., { children: true })`, preserving effect-specific pre-destroy reset behavior. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


- Small particle-manager display cleanup: routed `DropThroughDust`, `DashBoostPuff`, `IceSkidStreak`, `CriticalHighlight`, `AshRemnant`, and `HitBloodSpray` cleanup through `DisplayObjectLifecycleHelpers`, preserving `DashBoostPuff` detach-only semantics and destroy semantics for the other Graphics particles. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


- Dust/puff effect display cleanup: routed `FootstepPuff`, `JumpTakeoffPuff`, `WallSlideDust`, `WallJumpDust`, `WaterBubbles`, and `SteamPuff` cleanup through `DisplayObjectLifecycleHelpers`; preserved detach-only dust semantics and destroy semantics for water/steam particles. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


- Burst VFX Graphics display cleanup: routed `DeathParticles`, `DoubleJumpRing`, `DiveLandImpact`, `FlaskHealBurst`, `ItemPickupGlow`, and `WaterSplash` expired/cleared Graphics through `DisplayObjectLifecycleHelpers.destroyDisplayObject()`, preserving existing destroy semantics. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


- Independent effect destroy cleanup: routed `AbsorbParticles`, `DashAfterimage`, `EmberRise`, `LowHpVignette`, `SmokeWisp`, and `HitSpark` display destruction through `DisplayObjectLifecycleHelpers.destroyDisplayObject()`, preserving existing particle/root destroy semantics. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


- Additional VFX lifecycle cleanup: routed `RelicAuraBurst`, `SavepointPulse`, `SurgeVfx`, `PropShatter`, `MemoryDive`, and `LandingDust` display cleanup through `DisplayObjectLifecycleHelpers`; preserved `MemoryDive` particle detach-only, root destroy, and `LandingDust.clear()` context destroy options. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


- Effect root/overlay destroy cleanup: routed `AnvilGateLaser`, `EchoPlayer`, `ItemWorldForgeBirth`, `ItemWorldLeakageLayer`, `PileDriver`, `WorldPullIn`, and `VoidDrop` owned display destruction through `DisplayObjectLifecycleHelpers`, preserving texture cleanup and `VoidDrop` child-before-root destroy ordering. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


- Mid-size effect display cleanup: routed `EgoShard`, `FluidResidue`, `FluidCrestFoam`, `GrassClumpFire`, `ScreenCrack`, and `ItemWorldEntrySequence` display cleanup through `DisplayObjectLifecycleHelpers`; preserved destroy ownership and left FluidCrestFoam CSV SSoT behavior unchanged. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


- Continued display-object lifecycle cleanup by normalizing small entity root destroy paths and the missed `FluidResidue` blot cleanup paths through `destroyDisplayObject`; gameplay/data behavior unchanged. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


- Normalized the second entity/item/effect display cleanup batch through `detachDisplayObject`/`destroyDisplayObject`, preserving detach-only versus destroy-owning semantics. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


- Normalized small system/effect/renderer display cleanup through shared lifecycle helpers, preserving pool detach and renderer texture/context destroy options. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


- Normalized remaining `FluidSystem` body/drop display teardown through `destroyDisplayObject`; fluid simulation and foam SSoT behavior unchanged. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


- Reduced world/itemworld container-fluid runtime duplication by moving shared BFS fluid painting into `ContainerFluidHelpers.paintContainerFluidCells()` while preserving mode-specific ignition/flush policy. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


- Reduced world/itemworld pickup reward duplication by moving gold/healing feedback into `PickupCollectionHelpers`, preserving World persistence and Item World heal glow/conditional toast behavior. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


- Reduced World/Item World movement VFX manager-update duplication with `MovementVfxHelpers.updateCommonMovementVfxManagers()`, preserving World late-update policy and Item World fluid-residue/enemy-fluid behavior. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


- Reduced World/Item World spike player-hit duplication with `TileHazardRuntimeHelpers.applyPlayerSpikeHitFeedback()`, preserving World rumble/death-hitstop options and Item World tile-spike detection policy. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


- Reduced LDtk World/Item World enemy contact damage duplication by adding `EnemyContactDamageHelpers.applyEnemyContactDamageForPlayer()`; legacy `WorldScene` attack-gated contact path remains separate. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


- Reduced World/Item World static-entity feedback duplication by adding `StaticEntityFeedbackHelpers` for cracked floor, switch, and gate feedback while preserving runtime-specific collision, analytics, rumble, and list ownership. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.


- Moved LDtk World enemy body-contact damage orchestration into `WorldEnemyContactRuntime`, keeping scene update order and shared contact-damage feedback semantics aligned with Item World. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.

- Moved LDtk World player attack hit resolution into `WorldEnemyCombatRuntime`, keeping locked-door attack blocking, `HitManager.checkHits()`, damage numbers, hit sparks, SFX, and heavy-hit flash semantics intact. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.
- Moved LDtk World enemy `update(dt)` iteration into `WorldEnemyUpdateRuntime`, keeping `LdtkWorldScene` responsible for runtime invocation order. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.
- Moved LDtk World enemy `render(alpha)` iteration into `WorldEnemyRenderRuntime`, keeping `LdtkWorldScene` responsible for render alpha resolution and broader render order. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.
- Moved LDtk World fluid arc/steam/electric reaction callback wiring into `WorldFluidReactionRuntime`, keeping `LdtkWorldScene` responsible for initialization timing and wall tile rerender invalidation. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.
- Added `syncPlayerAndEnemyPreviousPositions()` to `PlayerPlacementHelpers` and replaced LDtk World transition/post-snap spread calls, keeping player/enemy previous-position sync semantics explicit. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.
- Moved LDtk World procedural grass clump burnable registration loop into `WorldGrassFireRuntime.registerProceduralBurnables()`, keeping level-load registration timing in `LdtkWorldScene`. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.
- Tightened `WorldEnemyKillRuntime.processDefeatedEnemies()` so it reads enemies through injected `getEnemies`; `LdtkWorldScene` no longer passes `this.enemies` at the callsite. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.
- Moved `FluidSystem.onEvaporated` residue-drop handling into `WorldFluidReactionRuntime`, leaving `LdtkWorldScene` with only wall-tile rerender invalidation among fluid/tile reaction callbacks. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.
- Removed low-value `LdtkWorldScene` pass-through getters for held container, savepoint pulse, and movement VFX managers; callsites now use the owning runtimes directly. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.
- Removed additional low-value `LdtkWorldScene` pass-through getters for pickup VFX and prop shatter feedback; callsites now use owning runtimes directly. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.
- Removed remaining `LdtkWorldScene` combat feedback pass-through getters for damage numbers, hit sparks, and screen flash; callsites now use `WorldCombatFeedbackRuntime` directly. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.
- Removed `LdtkWorldScene` progress-state pass-through getters for visited/cleared/collected/unlocked sets; callsites now reference `WorldProgressState` directly. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.
- Removed `LdtkWorldScene` `worldMap` pass-through getter; callsites now use `WorldMapRuntime.overlay` directly. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.
- Removed `LdtkWorldScene` `minimap` pass-through getter; callsites now use `WorldMinimapRuntime.container` and `setVisible()` directly. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.
- Removed `LdtkWorldScene` `tileMutator` pass-through getter; callsites now use `WorldTileMutationRuntime.mutator` directly. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.
- Removed `LdtkWorldScene` enemy/container registry pass-through getters; dependency wiring now references `WorldEnemyRegistry.enemies` and `WorldContainerRegistry.containers` directly. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.
- Removed `LdtkWorldScene` fluid manager pass-through getters; callsites now use `WorldFluidRuntime.system/spawners/crestFoam/residue` directly. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.
- Removed `LdtkWorldScene` `collisionGrid` pass-through getter; callsites now use `WorldCollisionGridRuntime.grid` directly while preserving DEC-042 live-grid ownership. Verified with `npx tsc --noEmit` and `npm run build` from `game/`; build retains the existing LDtk atlas/prologue_01.png CSV warning only.

- Routed remaining direct DisplayObject removal sites through DisplayObjectLifecycleHelpers: SceneManager detach, ending overlay disposal, entity/UI child cleanup, inventory tab/shell clears. Verified with rg for .removeChild, npx tsc --noEmit, and npm run build (existing V3 prologue_01 atlas warning only).

- Removed pass-through private getters from EgoShardRuntime and WorldBuilderPlayerCollisionRuntime. Runtime access now uses explicit require* guards or injected deps directly. Verified with rg for private getter/current pass-through remnants, npx tsc --noEmit, and npm run build (existing V3 prologue_01 atlas warning only).

- Promoted fluid reaction callback ownership from world-only WorldFluidReactionRuntime to shared FluidReactionRuntime. LdtkWorldScene and ItemWorldScene now bind the same runtime; ItemWorldScene no longer owns direct FluidSystem arc/evaporation or TileMutator steam/electric callbacks. Verified with rg and npm run build (existing V3 prologue_01 atlas warning only).

- Moved ItemWorldScene breakable prop sway update loop into ItemWorldBreakablePropRuntime.update(), reusing shared updateBreakableProps so the scene no longer iterates breakableProps for per-frame animation. Verified with rg, npx tsc --noEmit, and npm run build (existing V3 prologue_01 atlas warning only).

- Replaced legacy WorldScene enemy render loop with shared renderEnemies helper. Left update/death/drop/contact loops in place because they still combine gameplay side effects. Verified with rg, npx tsc --noEmit, and npm run build (existing V3 prologue_01 atlas warning only).

- Replaced legacy WorldScene cleanup loops for enemies, projectiles, drops, portals, and altars with shared clearEnemyRegistry/destroyAndClearEntities helpers. Verified with rg, npx tsc --noEmit, and npm run build (existing V3 prologue_01 atlas warning only).

- Added detachAndClearDisplayObjects to DisplayObjectLifecycleHelpers and used it for legacy WorldScene door marker cleanup, removing another scene-local display detach loop. Verified with rg, npx tsc --noEmit, and npm run build (existing V3 prologue_01 atlas warning only).

- Replaced legacy WorldScene's direct Ghost pending projectile drain loop with shared collectPendingGhostProjectiles, matching World/ItemWorld projectile collection boundaries. Verified with rg, npx tsc --noEmit, and npm run build (existing V3 prologue_01 atlas warning only).

- Replaced legacy WorldScene projectile update/removal loop with shared updateProjectileCollection; player-hit feedback remains in a scene callback. Verified with rg, npx tsc --noEmit, and npm run build (existing V3 prologue_01 atlas warning only).

- Extracted legacy WorldScene active melee enemy attack damage loop into shared EnemyMeleeAttackDamageHelpers.applyEnemyMeleeAttackDamageForPlayer, preserving Skeleton/GoldenMonster active-attack predicate and Sakurai feedback constants. Verified with rg, npx tsc --noEmit, and npm run build (existing V3 prologue_01 atlas warning only).

- Extracted player attack hit feedback loop into shared PlayerAttackHitFeedbackHelpers.applyPlayerAttackHitFeedback and wired legacy WorldScene, WorldEnemyCombatRuntime, and ItemWorldEnemyCombatRuntime through it. World runtime keeps milestone disabled; legacy WorldScene and ItemWorld keep 100-damage milestone enabled. Verified with rg, npx tsc --noEmit, and npm run build (existing V3 prologue_01 atlas warning only).

- Replaced legacy WorldScene item-drop pickup loop with shared processPickupsForPlayerCollection. The helper now allows onPickup to return false to preserve failed pickup semantics and supports pickup types without width/height when a custom isNearPlayer is supplied. Verified with rg, npx tsc --noEmit, and npm run build (existing V3 prologue_01 atlas warning only).

- Replaced legacy WorldScene room-clear alive enemy filter with shared countAliveEnemies helper. Combat target filtering remains local to checkHits target selection. Verified with rg, npx tsc --noEmit, and npm run build (existing V3 prologue_01 atlas warning only).

## Legacy WorldScene Enemy Death Drop Extraction

- `game/src/scenes/WorldScene.ts` enemy update loop now delegates newly-dead enemy side effects to `processEnemyJustDied(enemy)`.
- Behavior is intentionally unchanged: death particles, golden/normal drop roll, drop spawn resolution, entity insertion, and `trackItemDrop` telemetry remain in the same order.
- Verification: `npx tsc --noEmit`; `npm run build` (passes with existing `[V3] LDtk uses "atlas/prologue_01.png"...` warning).

## Legacy WorldScene Enemy Update Loop Extraction

- `game/src/scenes/WorldScene.ts` now delegates the legacy enemy update/death/removal loop to `updateEnemies(dt)`.
- `update()` keeps the same execution order around player attack, ghost projectile collection, and projectile updates; only the enemy loop body moved.
- Verification: `npx tsc --noEmit`; `npm run build` (passes with existing `[V3] LDtk uses "atlas/prologue_01.png"...` warning).

## Legacy WorldScene Player Attack Feedback Extraction

- `game/src/scenes/WorldScene.ts` now delegates the player attack target filtering, hit check, and hit feedback application to `updatePlayerAttackHitFeedback()`.
- Ordering is unchanged: this still runs after enemy update/death/removal and before ghost projectile collection/projectile updates.
- Verification: `npx tsc --noEmit`; `npm run build` (passes with existing `[V3] LDtk uses "atlas/prologue_01.png"...` warning).

## Legacy WorldScene Projectile Update Extraction

- `game/src/scenes/WorldScene.ts` now delegates ghost projectile collection and projectile update/removal to `updateProjectiles(dt)`.
- The call remains immediately after player attack hit feedback, preserving prior combat ordering.
- Verification: `npx tsc --noEmit`; `npm run build` (passes with existing `[V3] LDtk uses "atlas/prologue_01.png"...` warning).

## Legacy WorldScene Enemy Movement VFX Extraction

- `game/src/scenes/WorldScene.ts` now delegates the legacy enemy water/bubble/ice/landing/jump VFX loop to `updateEnemyMovementVfx(dt)`.
- This intentionally stays inside `WorldScene` rather than `MovementVfxHelpers`, preserving the existing wiki boundary that enemy/environment VFX should remain scene/runtime-owned.
- Verification: `npx tsc --noEmit`; `npm run build` (passes with existing `[V3] LDtk uses "atlas/prologue_01.png"...` warning).

## Proximity Interaction Helper Extraction

- Added `game/src/scenes/shared/ProximityInteractionHelpers.ts` for shared portal/altar proximity, hint, and interact iteration.
- Wired legacy `WorldScene`, LDtk `PortalRuntime`, and `WorldAltarController` through the helper while keeping portal detach/destroy, transition setup, altar UI, and item validation caller-owned.
- Verification: `npx tsc --noEmit`; `npm run build` (passes with existing `[V3] LDtk uses "atlas/prologue_01.png"...` warning).

## Legacy WorldScene Altar Localization Cleanup

- `game/src/scenes/WorldScene.ts` altar item rows now use `ui.altar.item_row`, `ui.altar.equipped_suffix`, and `getDisplayName(item)` instead of hardcoded item UI strings.
- This aligns legacy altar UI with `WorldAltarController` and the `Sheets/Content_Localization.csv` SSoT rule.
- Verification: `npx tsc --noEmit`; `npm run build` (passes with existing `[V3] LDtk uses "atlas/prologue_01.png"...` warning).

## Item Selection Input Helper Extraction

- Added `game/src/scenes/shared/ItemSelectionInputHelpers.ts` for shared item selection up/down/confirm/cancel input flow.
- Wired legacy `WorldScene` altar UI and `WorldAltarController` through the helper while keeping item validation, altar mutation, portal spawning, and UI rendering caller-owned.
- Verification: `npx tsc --noEmit`; `npm run build` (passes with existing `[V3] LDtk uses "atlas/prologue_01.png"...` warning).

## Altar Item Selection UI Helper Extraction

- Added `game/src/scenes/shared/AltarItemSelectionUiHelpers.ts` for shared localized altar item row creation.
- Wired legacy `WorldScene` altar UI and `WorldAltarController` through `addAltarItemRows(...)`; callers still own panel/title styling, UI lifetime, validation, and portal spawning.
- Verification: `npx tsc --noEmit`; `npm run build` (passes with existing `[V3] LDtk uses "atlas/prologue_01.png"...` warning).

## ItemWorld AABB Grid Clear Helper Extraction

- Added `isAabbClearOfSolidTiles(...)` to `game/src/core/Physics.ts` as a public wrapper over the existing solid AABB overlap logic.
- Removed `ItemWorldScene.isAabbClearInGrid(...)`; `ItemWorldEntryCorridorRuntime` wiring now calls the core helper directly, preserving out-of-bounds-as-solid behavior.
- Verification: `npx tsc --noEmit`; `npm run build` (passes with existing `[V3] LDtk uses "atlas/prologue_01.png"...` warning).

## ItemWorld Full Map Population Helper Extraction

- Added `game/src/scenes/itemworld/ItemWorldFullMapPopulationHelpers.ts` for unified-grid room iteration, deterministic per-cell PRNG creation, room offset calculation, and room-count return.
- `ItemWorldScene.buildFullMap()` now keeps room type assignment, full-grid collision, reward spawner capture, cell visual records, player spawn capture, and current-cell visit mutation in the callback while removing the nested scene-local grid loop.
- Verification: `npx tsc --noEmit`; `npm run build` (passes with existing `[V3] LDtk uses "atlas/prologue_01.png"...` warning).

## Container Target Helper Extraction

- Added `game/src/scenes/shared/ContainerTargetHelpers.ts` for nullable Pixi `Container` target compaction.
- Replaced repeated inline `filter((target): target is Container => !!target)` in `LdtkWorldScene` runtime wiring and `WorldBuilderFlowRuntime.getBuilderAtmosphereTargets()`.
- Verification: `npx tsc --noEmit`; `npm run build` (passes with existing `[V3] LDtk uses "atlas/prologue_01.png"...` warning).

## LDtk World Tileset Path Collection Cleanup

- `game/src/scenes/LdtkWorldScene.ts` now reuses `collectLdtkTilesetPaths(...)` for LDtk/builder tileset preloading instead of owning a scene-local nested tile flattening loop.
- Level collection remains scene-owned because it combines the main world loader and builder loader.
- Verification: `npx tsc --noEmit`; `npm run build` (passes with existing `[V3] LDtk uses "atlas/prologue_01.png"...` warning).

## World LDtk Tile Filtering Helper Extraction

- Added `game/src/scenes/world/WorldLdtkTileFilterHelpers.ts` for collision-grid-based LDtk wall tile filtering.
- `LdtkWorldScene` now uses the helper for initial level render and wall-layer rerender, preserving slope visual stamps and the initial-render water-cell exclusion.
- Verification: `npx tsc --noEmit`; `npm run build` (passes with existing `[V3] LDtk uses "atlas/prologue_01.png"...` warning).

## LDtk Area Retag Helper Extraction

- Added `game/src/level/LdtkAreaRetagHelpers.ts` for default-authored world BG/WALL/optional shadow retagging.
- `LdtkWorldScene` and `GiantBuilder` now use `applyDefaultWorldAreaRetags(...)`, preserving the rule that only `atlas/world_01.png` authored tiles are retagged and override tilesets stay intact.
- Verification: `npx tsc --noEmit`; `npm run build` (passes with existing `[V3] LDtk uses "atlas/prologue_01.png"...` warning).

## LDtk Authored Tileset Preload Helper Extraction

- Added `preloadMissingLdtkTilesets(...)` to `game/src/level/LdtkTilesetPaths.ts` so authored LDtk tileset path collection and missing-atlas loading share one helper.
- `ItemWorldEntryPreloader` now delegates its authored tileset load loop to the helper while preserving its warning policy through `onLoadError`.
- Verification: `npx tsc --noEmit`; `npm run build` (passes with existing `[V3] LDtk uses "atlas/prologue_01.png"...` warning).

## World Builder Entity Dispatch Helper Extraction

- Added `game/src/scenes/world/WorldBuilderEntitySpawnHelpers.ts` for ordered builder LDtk entity dispatch.
- `WorldBuilderFlowRuntime` now supplies item/static/switch/entrance/anvil/sprite handlers to the helper while keeping handler order and entity-specific spawn policy in the existing runtimes.
- Verification: `npx tsc --noEmit`; `npm run build` (passes with existing `[V3] LDtk uses "atlas/prologue_01.png"...` warning).

## Legacy WorldScene Mini-Map Rendering Extraction

- Extracted legacy procedural minimap drawing logic from `WorldScene.drawMiniMap()` into `game/src/scenes/shared/WorldMiniMapHelpers.ts`.
- `drawMiniMap()` now delegates container redraw to `drawLegacyWorldMiniMap(...)` with the same map dimensions, spacing, and current-room highlighting rules.
- Added to reduce scene-local render boilerplate in `WorldScene` while preserving visible minimap behavior.

## Legacy WorldScene Door Transition Extraction

- Added `game/src/scenes/shared/WorldTransitionHelpers.ts` with `findDoorTransitionCandidate(...)`.
- `WorldScene.checkDoorTriggers()` now uses the shared helper for overlap check + next-room validation, keeping fade/order/state mutation in scene-local callsite.
- This keeps transition side effects in `WorldScene` while centralizing the direction/bounds/cell-type gating logic.

## Pass-through Cleanup (World/Item World flow continuation)
- WorldFixedItemWorldFlowRuntime.ts, WorldPortalItemWorldFlowRuntime.ts, WorldEdgeTransitionFlowRuntime.ts, and WorldAnvilInteractionRuntime.ts now avoid trivial scene-style pass-through access patterns previously duplicated in call sites.
- LdtkWorldScene.ts now reads fixed-item-world state/ownership through dedicated dependencies (fixedItemWorld, anvilPrompts, etc.) instead of routing through legacy passthrough getters.
- Follow-up cleanup in this pass: removed duplicate Inventory import in WorldPortalItemWorldFlowRuntime.ts.

## ItemWorldScene Compile Surface Cleanup

- Removed remaining scene-local pass-throughs for room state/spawn, flow-state start, updraft/absorb, and Ego dialogue forwarding in `game/src/scenes/ItemWorldScene.ts`.
- Re-aligned Item World runtime injection contracts (`getCollisionGrid`, saveAccess, container removal callbacks) and world container registry access through `getContainers()`.
- Tightened `InventoryVisibilityStatePolicy` binding types so transition runners no longer need stale `visible`/`mode` fields.
- Verification: `npx tsc --noEmit`; `npm run build` passed with the existing LDtk atlas/prologue_01.png warning.

## ItemWorldScene Boss Metadata Cleanup

- Replaced direct boss metadata access in `game/src/scenes/ItemWorldScene.ts` with `EnemyMetadata` helpers for boss detection and portal-spawn marking.
- Verification: `npx tsc --noEmit`; `npm run build` passed with the existing LDtk atlas/prologue_01.png warning.

## ItemWorldScene Gameplay Timer Migration

- Wired `ItemWorldBossClearRuntime` into `ItemWorldScene` and moved entry dialogue, boss-clear follow-up burst, and trapdoor spawn delays from `setTimeout()` to dt-driven runtime scheduling.
- `ItemWorldScene.ts` now has no `setTimeout(` matches; remaining project timers are audio, data-load timeout, or UI focus paths.
- Verification: `npx tsc --noEmit`; `npm run build` passed with the existing LDtk atlas/prologue_01.png warning.

## ItemWorldScene Player Collision Binding Cleanup

- Replaced remaining direct `this.player.roomData = ...` writes in `game/src/scenes/ItemWorldScene.ts` with `bindPlayerCollisionGrid(...)`.
- Left scene-owned `this.roomData = ...` assignments unchanged because they are the active collision-grid ownership seam.
- Verification: `npx tsc --noEmit`; `npm run build` passed with the existing LDtk atlas/prologue_01.png warning.

## ItemWorld Breakable Prop Update Ownership

- Replaced the remaining scene-local breakable prop update loop in `game/src/scenes/ItemWorldScene.ts` with `ItemWorldBreakablePropRuntime.update(dt)`.
- Verification: `npx tsc --noEmit`; `npm run build` passed with the existing LDtk atlas/prologue_01.png warning.

- Continued ItemWorldScene refactor: replaced direct fluid reaction callback implementation with shared FluidReactionRuntime binding, leaving only wall-tile invalidation in the scene. Verified with npx tsc --noEmit and npm run build; build still reports the existing LDtk atlas/prologue_01.png CSV warning.

- Continued ItemWorldScene refactor: moved defeated-boss metadata consume/gating into ItemWorldBossClearRuntime.consumeDefeatedBoss(...). Scene retains concrete boss-clear reward/save/dialogue/trapdoor side effects. Verified with npx tsc --noEmit and npm run build; build still reports the existing LDtk atlas/prologue_01.png CSV warning.

- Continued ItemWorldScene cleanup: routed HUD/lore/area-title/screen-flash detach paths through DisplayObjectLifecycleHelpers.detachDisplayObject(...). Verified rg .removeChild shows only helper internals in checked scene/UI/shared targets, plus npx tsc --noEmit and npm run build; build still reports the existing LDtk atlas/prologue_01.png CSV warning.

- Continued R4 PauseMenu cleanup: added PauseMenuPulse.advancePauseMenuPulseStates(...) and replaced repeated confirm/preset/settings/audio pulse advance calls in PauseMenu.update(). Verified with npx tsc --noEmit and npm run build; build still reports the existing LDtk atlas/prologue_01.png CSV warning.

- Continued R5 escape-hatch cleanup: added core/AsyncTimeout.raceWithTimeout(...) and replaced boot font-load plus asset prewarm timeout races. Left audio timers and DeferredFocus as intentional domain escape hatches. Verified rg setTimeout boundary, npx tsc --noEmit, and npm run build; build still reports the existing LDtk atlas/prologue_01.png CSV warning.

- Reviewed remaining parallel runtime candidates ContainerCarry and Pickup. Current code already routes common state/tether or collection/reward behavior through shared helpers; mode-specific prompt/lifecycle/persistence/healing ordering remains local by design. No code change needed for these pairs.

- Continued Phase 3 runtime sync: moved shared projectile-vs-player hit guard/overlap/feedback/kill path into ProjectileCollisionHelpers.tryHitPlayerWithProjectile(...). World and ItemWorld projectile runtimes retain only deflect policy differences. Verified with npx tsc --noEmit and npm run build; build still reports the existing LDtk atlas/prologue_01.png CSV warning.

- Reviewed PlayerSpawn, Weather, and EgoShard runtime pairs. No code change needed: shared helpers/adapters already cover equivalent behavior, while spawn policy, weather source/profile policy, and EgoShard side-effect routing remain intentionally mode-owned.

- Reviewed ProceduralDecor, TileHazard, PrologueEnd, and EgoDialogue runtime pairs. No code change: shared helpers cover equivalent behavior where safe, while mode-specific generation, cleanup side effects, trigger sequencing, and dialogue policies remain local.

- Removed WorldIntroHandoffRuntime -> TitleScene constant coupling by moving TITLE_FADE_OVERLAY_LABEL into scenes/shared/TitleHandoffLabels.ts. Verified scene import scan, npx tsc --noEmit, and npm run build; build still reports the existing LDtk atlas/prologue_01.png CSV warning.

- Continued projectile cleanup: routed legacy WorldScene projectile-vs-player hit handling through ProjectileCollisionHelpers.tryHitPlayerWithProjectile(...) using behavior-preserving options. Verified with npx tsc --noEmit and npm run build; build still reports the existing LDtk atlas/prologue_01.png CSV warning.

- Completed refactor roadmap audit. Marked RefactorRoadmap status completed and recorded that remaining/candidate lines are superseded by current worktree evidence: runtime ownership seams are closed or intentionally mode-owned, UI policy/display work is delegated, escape hatches are bounded, no true as-any/TODO/FIXME remain, and latest npx tsc --noEmit plus npm run build pass with only the known LDtk atlas/prologue_01.png CSV warning.

- Fixed anvil dive regression: InventoryInteractionPolicy now returns confirm-anvil for ATTACK in anvil mode, and InventoryUI.handleAttackInput() routes it to confirmSelected() while returning none to scene-level equipment-change handling. This restores place item -> confirm dive on anvil. Verified with npx tsc --noEmit and npm run build; build still reports the existing LDtk atlas/prologue_01.png CSV warning.

## 2026-06-05 - Cross-agent wiki instruction

- Added `AGENTS.md` guidance that other LLM coding agents may search, read, and update `memory/wiki/` during normal project work.
- Documented the required workflow: start from `memory/wiki/WIKI_INDEX.md`, open relevant wiki pages, prefer concise appends, update the index for new pages, and avoid wholesale rewrites of encoding-sensitive legacy wiki files such as `memory/wiki/features/RefactorRoadmap.md`.

## 2026-06-05 - Zoom-aware world prompt projection

- Added `game/src/scenes/shared/WorldPromptProjection.ts` to mirror the renderer's zoomed RenderTexture world-to-screen projection for UI-container prompts.
- Routed LDtk dialogue prompts, anvil prompts, Item World world prompts, and frozen-return prompts through the helper so key glyph panels stay anchored during camera zoom in/out.

## 2026-06-05 - Start-world attack gate

- Added `Player.attackInputEnabled` to gate player attack and dive-attack input.
- LDtk world player creation disables attack input at game start; Item World player creation enables it when cloning the source player so combat is available after entering Item World.

## 2026-06-05 - Item World entry tunnel zoom lock

- Changed `ItemWorldEntrySequence` to lock camera zoom at 1.0 during the default Item World growth/tunnel entry and unlock at the final ItemWorldScene handoff while preserving 1.0 zoom.

