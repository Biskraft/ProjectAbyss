# 2026-06-07

## Projectile debug visualization
- Added Shift+I visuals for enemy projectile diagnosis: enemy center-to-target LOS ray, Lobber locked bomb arc, and projectile center collision point.

## Projectile LOS solid predicate
- Changed enemy projectile LOS and projectile center collision from `TILE_WALL` only to `isSolid()` so Shift+I ray debug and projectile destruction agree with solid IntGrid cells.

## Bulwark active attack box
- Added Bulwark active attack placeholder box/effect and fixed shield art double-inversion.
- Routed active enemy melee damage through world/itemworld contact runtimes via `enemy.isAttackActive()` before generic contact damage.

## Bulwark attack timing retune
- Retuned Bulwark to 500ms guarded telegraph, immediate 40x32 active attack box, then 1500ms unguarded vulnerable anticipate.
- Added `Enemy.getAttackAABB()` and made shared enemy melee damage use it when present so Bulwark's visible attack box matches actual damage.

## Bulwark telegraph and turn timing
- Reduced Bulwark guarded telegraph to 250ms and added a 1000ms delayed combat turn so it no longer instantly flips toward the player.

## Bulwark delayed turn enforcement
- Replaced Bulwark chase `moveTowardTarget()` call with direct `vx` movement so base movement no longer instantly flips `facingRight`.

## Bulwark instant flip follow-up
- Removed the remaining Bulwark chase `moveTowardTarget()` call that was still mutating `facingRight` instantly. Chase now directly sets `vx`; facing is controlled only by the delayed turn helper outside patrol.

## Bulwark guard-facing split
- Split Bulwark shield/attack direction into `guardFacingRight`, with 1000ms delayed turn controlling shield art, block direction, attack AABB, and rendered facing together.

## Player hit knockback tuning
- Doubled player knockback velocities on damage intake without changing hitstun/invincibility: enemy contact/melee `100,-50 -> 200,-100`, projectile `80,-40 -> 160,-80`, falling hazard `0,-120 -> 0,-240`.

## Player hit facing and knockback tuning
- Player `onHit()` now preserves the pre-hit `facingRight` after transitioning to hit state so damage knockback does not turn Erda around.
- Doubled hit knockback again for total 4x original: enemy contact/melee `400,-200`, projectile `320,-160`, falling hazard `0,-480`. Hitstun/invincibility remain unchanged.

## Player hit facing follow-up
- Changed Player `_hitKnockDir` on hit to preserve pre-hit facing direction instead of knockback sign. This keeps hit animation/VFX direction from turning Erda around while physical knockback still uses the incoming knockback vector.
