# 2026-06-06

## Enemy content/stat split

- Added `Sheets/Content_Enemy.csv` for enemy behavior tuning.
- Reduced `Sheets/Content_Stats_Enemy.csv` to simple stat/reward fields: `Type`, `Level`, `HP`, `ATK`, `DEF`, `Exp`.
- Reworked `game/src/data/enemyStats.ts` to merge stat and behavior CSV rows while preserving the existing `getEnemyStats()` API.
- Simplified `Content_Enemy.csv` to one row per enemy `Type`; `Level` remains only in `Content_Stats_Enemy.csv`.

## MawDrone flying charger

- Added `game/src/entities/MawDrone.ts` as a flying A-01 charger: patrol, detect, direct flying chase, 300ms melee tell, short active hit window, cooldown, and lose-target fallback.
- Registered `MawDrone` in `EnemyFactory` so LDtk/spawn-table enemy type strings can instantiate it.

## LDtk external enums

- Added `game/public/assets/World_ProjectAbyss_ExternalEnums.json`.
- Converted LDtk `ItemId` and `MonsterType` definitions in `game/public/assets/World_ProjectAbyss.ldtk` to external enums.
- `ItemId` values come from `Sheets/Content_Item_Master.csv`; `MonsterType` values come from `Sheets/Content_Enemy.csv`.

## Enemy archetype task-list baseline implementation

- Added `game/src/entities/ArchetypeEnemies.ts` with spawnable first-pass implementations for `Lobber`, `Bulwark`, `SparkBat`, `CinderImp`, `Lurker`, `Conduit`, and `Sentry`.
- Registered all new enemy types in `EnemyFactory`.
- Added their level stats to `Sheets/Content_Stats_Enemy.csv`, behavior rows to `Sheets/Content_Enemy.csv`, and `MonsterType` entries to `World_ProjectAbyss_ExternalEnums.json`.
- Baseline FSM/spawnability is implemented; advanced task-specific systems such as Lobber AoE, Bulwark directional damage reduction, Swarmer boids, and Summoner real minion/buff ownership still need dedicated combat/spawn hooks.

- LDtk world Shift+I debug floor text now includes current level id, unique Enemy_Spawn MonsterType values, direct Slime/Boss entity types, and NPC speaker/character names via game/src/scenes/LdtkWorldScene.ts.

- Implemented first-pass A-03b Lobber/Bombardier behavior: 500ms landing marker, arcing bomb, AoE blast, close-range retreat, and generic pending projectile collection for non-Ghost projectile enemies. Shift+I now also shows monsterType labels above LDtk world enemies.

- Enlarged Shift+I monsterType labels above enemies from 7px to 14px. Updated CinderImp from simple melee baseline to visible Swarmer behavior: skitter chase, leap attack, scatter recovery; pack spawning remains future spawn-runtime work.

- Fixed SparkBat flying away vertically: cooldown recovery now returns toward a player-near hover band instead of constantly moving upward after each dive.

- Added visible behavior payoffs for Conduit and Lurker. Conduit casts a 3-projectile burst and retreats from close range; Lurker reveals with a short tell and then performs a fast dash strike.

- Implemented real Conduit -> CinderImp summoning through a shared pendingSummons registry drain for world and Item World. Also fixed Bulwark shield facing by redrawing the shield on the active facing side.

- Added Bulwark directional shield gameplay. Front hits are reduced to 20% damage with a shield flash; rear hits deal 120%. HitManager now supports a target damage modifier hook for this kind of enemy-specific defense.

- Reworked Bulwark into a guard/anticipate shielder: raises shield after detecting the player, drops shield for a 1500ms vulnerable tell before attacking, then pushes and returns to guard. Unguarded windows now take 150% damage.

- Tuned Bulwark damage rule to hard shield front attacks: guarded front hits now deal 0 damage; rear and unguarded vulnerable windows deal normal 100% damage.

- Tuned Bulwark movement/timing: patrol and chase now use normal moveSpeed; attack startup is 500ms guarded telegraph followed by 1500ms unguarded vulnerable anticipate.

- Updated Lurker to use stealth ambush only once, then chase/attack normally. Inverted Bulwark shield art and front-block predicate together so visible shield side matches the actual guard side.

- Added runtime rendering for LDtk CommonSprite/commonSprite visual-only entities. They can use LDtk tile overrides or Sprite field assets under ssets/sprites/, including _atlas animated JSON sprites.

- Moved LDtk CommonSprite visuals from entityLayer to LdtkRenderer.interiorLayer so they render with object/interior visuals behind characters.

- Implemented prologue end cutscene handoff runtime. ItemWorld prologue end now pops back to LdtkWorldScene and starts WorldPrologueEndRuntime.startFromItemWorldHandoff() instead of immediately loading Ch.1. The runtime white-fades, bakes Prologue_Cinema_01 via temporary LdtkRenderer/RenderTexture, zooms the baked scene, then loads Start_Room_01 as chapter_01.

## Prologue cinematic handoff fixes
- Fixed Item World prologue return to perform white-out/white-in instead of skipping directly to white-in.
- Added player wake-up animation override after chapter_01 live handoff.
- Rebased baked cinematic zoom around the LDtk `Player` pivot, preferring `Scene=chapter_01`.
- Added persistent PowerShell `codex-done` / `Invoke-CodexWorkDoneSound` helper in CurrentUserAllHosts profile.

## Prologue cutscene Playwright tuning
- Added `?prologueCutscene` direct debug entry from boot to `LdtkWorldScene`.
- Used Playwright-controlled Chrome captures to tune the cinematic zoom and handoff pivot.
- Fixed baked cinematic entity rendering by using parsed LDtk `type` and `tile` fields, then adding `CommonSprite` to the RenderTexture bake.

## Prologue cutscene live LDtk rewrite
- Replaced baked grayscale RenderTexture transition with real `Prologue_Cinema_01` level loading and live camera zoom/pan.
- Confirmed via Playwright Chrome captures that the cutscene now uses live palette/rendering instead of a baked texture.
- Hid world UI and feedback overlays during the cinematic camera move.

## Prologue wake-gated handoff updates
- Changed cinematic zoom range to 0.1 -> 1.0.
- Added player wake frame 0 hold before and after Start_Room handoff; movement input triggers wake-up animation.
- Added runtime outer collision wall fill for `Prologue_Cinema_01`.

## Prologue cinematic wall visual fix
- Fixed `Prologue_Cinema_01` outer IntGrid supplement to also synthesize wall visual tiles and rebuild the wall layer.

## Prologue cinematic all-wall visual synthesis
- Extended cinematic wall visual synthesis from outer boundary cells to every `TILE_WALL` IntGrid cell missing a wall tile.

## Prologue cutscene reset to overlap fade
- Removed cinematic wall synthesis path from the transition and switched to Start_Room + fading Prologue_Cinema overlay.
- Overlay now lives in feedback overlay space and scales 0.1 -> 1.0 while alpha fades 1 -> 0.
