# 2026-05-30

## Session - Camera shake visual bounds

- Replaced active LDtk/ItemWorld shake edge cover with `game/src/level/VisualBoundsBleed.ts`.
- `Camera.setBounds(..., renderPadding)` now clamps render-only shake inside a 4-tile visual bleed while keeping gameplay bounds exact.
- `LdtkWorldScene` and `ItemWorldScene` generate mirrored edge-tile bleed layers and no longer toggle `BoundsGuard` during shake.
- Verification: `npx tsc --noEmit` and `npm run build` from `game/` pass. Existing warnings only.

## Session - Wall-push walk animation

- Updated `game/src/entities/Player.ts` so grounded idle/run and aim/lift walk cycles use movement input intent or velocity.
- Wall collision can zero `vx`; held horizontal input now keeps run animation/state active instead of flickering to idle.
- Verification: `npx tsc --noEmit` and `npm run build` from `game/` pass. Existing warnings only.

## Session - Item World birth FX

- Added `game/src/effects/ItemWorldForgeBirth.ts` for the anvil entry: white item silhouette, black clipped INTGRID preview, laser-burst hitstop, rightward glass crack, item shards, and map shards flying toward the tunnel.
- Wired `ItemDeploymentController` / `AnvilDeploymentFactory` so the birth FX starts during item zoom and strikes on `AnvilGateLaser.burst()` without pre-initializing `ItemWorldScene`.
- Updated `ItemWorldGhostOverlay` tile reveal so player-proximity platforms slide in from shard offsets before settling into place.
- Verification: `npm run build` from `game/`; Puppeteer boot smoke at `http://127.0.0.1:5178/play/?debug=1`; Pixi smoke render for `ItemWorldForgeBirth`; ghost overlay shard-settle smoke.

## Session - First IW Boss Inventory Hint

- Fixed `game/src/scenes/LdtkWorldScene.ts` so first-boss-clear inventory tutorial hints are flushed when a retire-after-boss anvil consumes the placed item instead of going through `reclaimItemFromAnvil()`.
- Prevention: if `showFirstItemWorldReturnInventoryHint()` defers because an item is still placed, any non-reclaim path that clears/retires the anvil must also clear `lastUsedAnvilItem` and retry the pending hint.
- Verification: `npm run build` from `game/` passes. Existing CSV/LDtk warnings only.

## Session - Rustborn Inventory Hint Completion

- Updated `game/src/scenes/LdtkWorldScene.ts` so equipping `sword_rustborn` dismisses both inventory tutorial hint ids, clears any pending inventory hint, and disables the HUD item-key pulse.
- Prevention: inventory tutorial completion must clear both visible `TutorialHint` state and deferred `pendingInventoryHint` state; dismissing only the visible panel can leave a delayed hint armed.

## Session - Container Top Landing Pose

- Updated `Player.forceGrounded()` with an optional pose snap and used it for `ThrowableContainer` top support in `LdtkWorldScene` and `ItemWorldScene`.
- Fixed container-top standing by refreshing external grounding before `Player.update()` when feet are already on the container top face; this prevents exact-touch frames from flipping into air/land animation.

## Session - Item World Laser Camera Framing

- Updated `game/src/effects/ItemDeploymentController.ts` so `ItemWorldForgeBirth` starts in front of the anvil laser source rather than at the current placed-item icon.
- Added laser-burst camera staging: pan the camera target 20 tiles right during the burst, then return to the player 500 ms after `AnvilGateLaser` reports done.

## Session - Item World Shard-Build Staging

- Updated `game/src/effects/ItemWorldForgeBirth.ts` so the item/internal grid no longer has a rectangular cyan frame, laser strike breaks the item into random-size shards, shards float after laser completion, then move right one-by-one into partial dark platform chunks.
- Updated `game/src/effects/ItemDeploymentController.ts` to release floating item shards shortly after `AnvilGateLaser` completes; proximity completion remains handled by `ItemWorldGhostOverlay`.
- Verification: `npm run build` from `game/` passes. Existing CSV/LDtk/font/chunk warnings only.

## Session - Item World IntGrid Shard Build

- Changed shard staging so laser-broken item pieces stay floating until player movement starts the streamed reveal.
- Disabled the screen-glass crack call during laser burst.
- Updated `ItemWorldGhostOverlay` / `LdtkWorldScene` so revealed ghost tiles stamp their IntGrid values into `collisionGrid` one at a time; `ItemWorldForgeBirth` no longer draws fake dark platform chunks.
- Verification: `npm run build` from `game/` passes. Existing CSV/LDtk/font/chunk warnings only.

## Session - Item World Sprite Shards

- Removed forge lightning-branch rendering from `ItemWorldForgeBirth`.
- Changed item-birth shards from white graphic polygons to actual item texture chunks, with slower low-amplitude floating motion.
- Slowed streamed platform reveal to 400 ms per tile scale-in and a 36 ms tile queue interval.
- Verification: `npm run build` from `game/` passes. Existing CSV/LDtk/font/chunk warnings only.

## Session - Item World Reveal Timing

- Slowed `ItemWorldGhostOverlay` tile reveal again to 800 ms per tile scale-in and a 72 ms tile queue interval.
- Slowed `ItemWorldForgeBirth` shard travel duration to 2080-3280 ms and kept shard alpha solid until the shard reaches its target, where it disappears.
- Verification: `npm run build` from `game/` passes. Existing CSV/LDtk/font/chunk warnings only.

## Session - Item World Haze Pulse Removal

- Forced `ItemWorldScene` stratum weather `breathing=false` so the full-screen cyan haze no longer pulses during Item World.
- Kept local motes and memory sparks active.

## Session - Item World Shard Darkening

- Updated `ItemWorldForgeBirth` moving weapon shards to duplicate to roughly 2x count, darken from sprite color toward black during travel, and shrink to scale 0 near the destination.
- Verification: `npm run build` from `game/` passes. Existing CSV/LDtk/font/chunk warnings only.

## Session - Anvil Placed Item Move-In

- Removed the placed anvil item white/absorption particle phase from `Anvil`.
- `ItemDeploymentController` now starts `ItemWorldForgeBirth` hidden, sends the placed item toward the laser-birth focus at `ItemPunch`, then reveals the forge item only after the anvil sprite has scaled up to the full preview size.
- `Anvil` keeps the item sprite colored while it scales from the placement size to the forge preview size and moves to that focus.
- Shattered `ItemWorldForgeBirth` pieces still stay floating until player movement releases them into the streamed platform build.

## Session - Item World Entry Color Restore

- `ItemWorldScene` now preserves the entry corridor grayscale/contrast look when the player falls into the actual Item World start room.
- The temporary color-matrix filter is applied to world/background layers only and fades back to normal color over 1000 ms, then removes itself; scene destroy also clears it from the shared background container.

## Session - Item World Weather Profiles

- Updated `WeatherSystem` stratum mode to use world-space, IntGrid-colliding particles instead of screen-relative floating motes.
- `ItemWorldScene` now passes the area palette `WeatherProfile` and binds the resolved Item World `fullGrid` as weather collision.
- Added `shadow` weather profile parsing and mapped `iw_echo_*` to shadow oil-drip weather.
- Prevention: Item World weather should remain local particles with `breathing=false`; do not bring back full-screen stratum haze.
- Verification: `npm run build` from `game/` passes. Existing CSV/LDtk/font/chunk warnings only.
