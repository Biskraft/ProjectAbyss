# 2026-06-01

- Narrowed `World_ProjectAbyss.ldtk` `Collisions` -> `Walls` autotile rule `uid=2156` to the 2x1 slope pattern `[0,0,-1,-1,-1,1,1,1,0]` and regenerated cached `autoLayerTiles` for that rule.
- Result: the rule now leaves only 20 authored wall-value 2x1 slope matches / 40 tile entries instead of painting broad 1x1 corner cases.
- Verification: `npm run build` from `game/` passes. Existing warnings remain the 4 LDtk/CSV tileset divergence warnings, unresolved `fonts/PressStart2P.woff2`, and the existing Vite large chunk warning.
- Fixed runtime rendering for the 2x1 slope tiles. `LdtkLoader` now preserves LDtk auto-rule metadata (`t`, `d[0]`, `d[1]`), and the world/item-world wall-tile filters keep `uid=2156` slope stamps before culling air-cell tiles. Verification: `npx tsc --noEmit` and `npm run build` pass.
- Removed right-angle procedural decoration on 2x1 slope visuals by excluding inferred slope support cells from `ProceduralDecorator.scanEdges()`. This suppresses grass, moss/clingers, surface overlay, structures, and micro decoration on slope support cells while leaving normal flat surfaces untouched. Verification: `npx tsc --noEmit` and `npm run build` pass.
- Extended 2x1 virtual slope traversal to ground-type enemies in `Enemy.update()` while keeping flying enemies on normal movement. Skeleton and GoldenMonster patrol edge probes now use slope-aware ground support.
- Added shared slope support helpers in `Physics.ts` and used them to keep procedural breakable props off 2x1 slope footprints.
- Fixed a regression where ground enemies could not cleanly jump after the slope change: `Enemy.update()` now computes Y pixel movement after wall-blocked jump assignment and does not let an upward takeoff inherit `rx.onSlope` as grounded. Verification: `npx tsc --noEmit` and `npm run build` pass.
- Reduced enemy slope capture aggressiveness after a stuck-monster playtest: ground enemies now try normal X pixel collision first, using 2x1 slope-aware X movement only as a fallback when the normal horizontal move collides. Verification: `npx tsc --noEmit` and `npm run build` pass.
- Fixed enemy wall-block jump accumulation after the pixel-sweep change. Frames where horizontal intent exists but pixel remainder rounds `moveX` to 0 use an ahead-wall probe instead of resetting `wallBlockedTimer`, and wall-height scanning now uses intended movement direction instead of stale facing. `npm run build` passes.
- Added a short horizontal carry for wall-block navigation jumps. The X collision that triggers the jump zeros `vx`, and ground enemy AI skips chase while airborne, so without carry the enemy can jump in place and land back at the same wall. Verification: `npx tsc --noEmit` and `npm run build` pass.
- Fixed streamed Item World entry ghost placement/backtracking. The ghost start now requires support plus a four-tile forward floor run, uses a collision-only backstop 9 tiles left of the start, and clamps player x at that wall while deployed. Also hardened Anvil inventory input by consuming modal confirm/cancel, restoring hidden UI before opening, and refreshing rejected anvil selections. Verification: `npx tsc --noEmit`, `git diff --check`, and `npm run build` pass.
- Tightened streamed Item World ghost start selection to solid floor tiles only. One-way/drop-through platform tiles are no longer valid for the start or first forward run, preventing Down+Jump from exposing mismatched entry collision.
- Fixed the actual Down+Jump false positive: `Player` now starts drop-through only when `isOnOneWayPlatform()` is true, instead of any grounded floor. Solid Item World entry floors no longer nudge the player down like one-way platforms.
- Changed streamed Item World grid extension to initialize new cells as air instead of walls. Only the ghost collision copy and collision-only backstop should add collision, preventing invisible wall cells around the 36 preview.
- Converted streamed Item World ghost collision from full solid-body stamping to surface-only one-way platforms. The 36 preview's item/world silhouette remains visual, while only exposed top surfaces support the player and can be dropped through.
- Limited the streamed Item World entry collision to the lower main-route floor of `ItemStratum_Level_36`. Start selection now avoids upper silhouette edges, and surface stamping ignores rows above the chosen start floor to prevent hidden platforms from recurring.
- Replaced the fragile surface-inference entry collision with a fresh explicit entry path. `ItemStratum_Level_36` now remains visual-only during the anvil growth preview; gameplay collision is a single one-way platform strip with a matching visible path line and a player-x clamp behind the start.
- Replaced the explicit-path fallback with the authored Level 36 flow. The default anvil growth now scales only the captured world snapshot first; after zoom completion it loads `ItemStratum_Level_36`, stamps that IntGrid into runtime collision unchanged, places the player at the LDtk `Player` entity, and reveals nearby tiles visually from scale 0 to 1 as the player moves.
- Suppressed the old world-right-edge handoff while `itemDeployment` is active, so the default growth entry exits only through Level 36's streamed right-edge trigger.
- Made the growth zoom target the LDtk `Player` entity in `ItemStratum_Level_36`: the controller now reads that authored target at zoom start and moves the live player toward its projected position during the zoom instead of teleporting only after zoom completion. Also added a temporary full-world collision clear during the deployment so only Level 36 collision remains once stamped.
