# 2026-06-04

## Refactor planning survey

- Surveyed the current `game/src` topology for main-development refactoring handoff.
- Added `memory/wiki/features/RefactorRoadmap.md` with baseline metrics, guardrails, prioritized work slices, and verification matrix.
- Key finding: `LdtkWorldScene.ts` and `ItemWorldScene.ts` remain the highest-leverage refactor targets; existing runtime boundaries should be extended instead of replaced.
- Prevention rule: keep `DEC-042` collision ownership, `DEC-039` Item World vertical dive behavior, localization CSV SSoT, and shared UI binding rules intact during refactors.

## Execution started

- Implemented `game/src/scenes/itemworld/ItemWorldEntryCorridorRuntime.ts`.
- `ItemWorldScene.ts` now delegates entry corridor orchestration state/camera/visibility logic and dialogue gating to the new runtime.
- Remaining `ItemWorldScene.ts` `R2` scope: trapdoor transition cleanup, room-state/progress ownership and parallel runtime contract hardening.

## ItemWorld R2 timer refactor completed

- Removed remaining `setTimeout` flows in `game/src/scenes/ItemWorldScene.ts`:
  - entry dialogue delay (250ms),
  - boss-clear follow-up burst (160ms),
  - post-boss trapdoor spawn delay (2500ms), including onboarding `EGO_BOSS_KILLED` gate.
- Added `game/src/scenes/itemworld/ItemWorldBossClearRuntime.ts` and wired it for boss-clear + entry dialogue sequencing, with `update()` tick + `destroy()` cleanup in scene lifecycle.
- Completed `ItemWorldScene.ts`/`ItemWorldUiController.ts` stratum clear cleanup pass:
  - removed stale panel-based suppression dependency on `hasStratumClearPanel`,
  - removed `ItemWorldStratumClearPanelRuntime` call paths,
  - updated flow contracts so `ItemWorldProgressController` now owns stratum transition snapshot + continue/exit callbacks only.
- Added runtime documentation pages:
  - `memory/wiki/features/ItemWorldBossClearRuntime.md`
  - `memory/wiki/features/ItemWorldEntryCorridorRuntime.md`
  - and updated `memory/wiki/WIKI_INDEX.md` links.
- R2 room/progress ownership cleanup:
  - Removed scene-local room/progress helper wrappers from `ItemWorldScene.ts` and inlined delegation to `ItemWorldRoomStateRuntime`/runtime callbacks:
    `countTotalRooms`, `restoreRoomState`, `persistRoomState`, `getCurrentCell`, `spawnEnemiesInRoom`, `clearEnemies`.
  - Directly wired remaining room/spawn/progress mutations to the owning runtime interfaces (`roomStateRuntime` and `roomSpawnRuntime`).
  - Updated `memory/wiki/features/RefactorRoadmap.md` first-pass status to reflect R2 room/progress delegation completion.
- ItemWorld trapdoor descent handoff completion:
  - Added `game/src/scenes/itemworld/ItemWorldTrapdoorFlowRuntime.ts`.
  - Bound `ItemWorldTrapdoorRuntime` activation to flow runtime start.
  - Moved trapdoor descent orchestration branches (final/intermediate) behind dedicated scene callbacks (`prepareTrapdoorDescent`, `handleFinalTrapdoorDescent`, `handleIntermediateTrapdoorDescent`) plus `ItemWorldScene`-local execution.
  - Added `memory/wiki/features/ItemWorldTrapdoorFlowRuntime.md` and linked it in `memory/wiki/WIKI_INDEX.md`.
  - Hardened flow runtime entry guard so inactive/consumed trapdoors are ignored on activation.
  - Refined guard to cover both `active=false` and `consumed=true` entity states (Trapdoor/FloatingItemDrop parity).
- Added explicit R2 finish marker in `RefactorRoadmap`:
  - `ItemWorldTrapdoorFlowRuntime` ownership and scene callback split are now considered complete under R2.
  - Remaining work is contract normalization in R3 (shared contract and post-handoff input gating standardization).
- Additional ItemWorld R2 cleanup executed in this pass:
  - `ItemWorldTrapdoorFlowRuntime` wiring now owns trapdoor descent branch side-effects directly via constructor callbacks (`prepare`, `final`, `intermediate`) and no longer relies on separate scene helper methods.
- Trapdoor teardown is now centralized through `disposeTrapdoor()` from build-map clear paths.
  - Scene-level `ItemWorldTrapdoorFlowRuntime` callback list now documents transition ownership more explicitly, while leaving flow timing, prompt ownership, and manual activation checks in the dedicated trapdoor runtime pair.
- Final R2 cleanup in this pass:
  - Removed `ItemWorldScene.updateEntryCorridor()` shim and moved per-frame corridor updates directly to `ItemWorldEntryCorridorRuntime`.
  - Removed unused `LdtkWorldScene.unlockDoors()` wrapper; callers now rely on `worldDoorSwitchInteractionRuntime` directly.

## Metadata typing cleanup (ongoing, in-session)

- Added `game/src/entities/EnemyMetadata.ts` to centralize enemy metadata markers used by both worlds:
  - `isBossEnemy`, `markBossEnemy`, `setBossKey`, `setUnlockTargetIids`,
    `markPortalSpawned`, `wasPortalSpawned`.
- Replaced `as any` metadata access in combat/kill/spawn/scene paths for:
  - `world` boss spawn/kill and container-hit pathways,
  - `itemworld` boss encounter/combat/clear-flow.

## Builder/UI cleanup (in-session, 2026-06-04 late)

- Completed `LdtkWorldScene.ts` builder ownership migration:
  - Added `game/src/scenes/world/WorldBuilderFlowRuntime.ts` and wired it in `game/src/scenes/LdtkWorldScene.ts`.
  - Moved `spawnBuilderFromSpawner`, `syncBuilderAttachments`, `setBuilderEntranceGlowAlpha`, `clearBuilder`, and `getBuilderAtmosphereTargets` handling into the runtime.
  - Removed corresponding scene-local methods and re-routed callsites through `builderFlowRuntime`.
- Completed ItemWorld full-map filter typing hardening:
  - Updated `game/src/scenes/itemworld/ItemWorldFullMapLayerRuntime.ts` to use `Filter[]` for `filters` assignments and accessor helpers (`getBrightness/getDepthBias`).
- Completed UI text type cleanup:
  - Updated `game/src/ui/InventoryUI.ts` title nodes to handle `createUiText` union return type (`BitmapText | Text`) safely (no `panel.__title` escape hatch).
- Meta docs consistency:
  - Removed stale `ItemWorldStratumClearPanelRuntime` link entry from `memory/wiki/WIKI_INDEX.md` after migration out of the scene path.

- Verification pass completed on 2026-06-04:
  - `game/` `npx tsc --noEmit` pass.
  - `game/` `npm run build` pass; CSV/localization pipeline also passed (one non-blocking LDtk atlas warning only: `prologue_01.png` code override intent).
- Verification rerun after final access-scope adjustment:
  - `npx tsc --noEmit` pass.
  - `npm run build` pass; same warning only (`atlas/prologue_01.png`).

- R5 metadata hardening update:
  - Extended `game/src/entities/EnemyMetadata.ts` with boss-HP UI marker state helpers (`markBossBarShown`, `wasBossBarShown`) and migrated `game/src/ui/BossHpRuntime.ts` to use them instead of local boss marker field mutations.
  - Migrated enemy metadata flags from `game/src/systems/EntityRuntimeMeta.ts` into `game/src/entities/EnemyMetadata.ts`; subsequent turns completed the migration by making all current callsites import directly from `EnemyMetadata`.
- World builder flow ownership documentation pass:
  - Added `memory/wiki/features/WorldBuilderFlowRuntime.md` for `game/src/scenes/world/WorldBuilderFlowRuntime.ts`.
  - Updated `memory/wiki/WIKI_INDEX.md` to include the new feature page.

## WorldScene portal transition refactor (late continuation)

- Added `game/src/scenes/world/WorldScenePortalItemWorldFlowRuntime.ts`.
- `WorldScene` now routes portal-triggered Item World entry through `ItemWorldEntryPushTransition` + this runtime instead of directly pushing `ItemWorldScene`.
- Kept side-effects on return inside delegated callbacks: attack refresh, level-up/item acquisition/atk-change toasts, upgrade SFX, and auto-save.

## World fixed fallback parity pass

- Updated `game/src/scenes/world/WorldFixedItemWorldFlowRuntime.ts` so missing authored fixed-item levels fallback to procedural `ItemWorldScene` and now apply `applyItemWorldSceneCompletionRewards` before return dialogue/retirement.
- Added `showToast` and `getPlayerAtk` callbacks to `WorldFixedItemWorldFlowRuntime` wiring in `game/src/scenes/LdtkWorldScene.ts` so fallback completion now emits the same level-up and ATK-delta toast behavior as other Item World return paths.
- Updated `memory/wiki/features/WorldFixedItemWorldFlowRuntime.md` to document the procedural fallback reward-postprocessing parity.

## ItemWorld completion UX consistency pass

- Normalized `game/src/scenes/WorldScene.ts` portal-item completion toast payloads to use `getDisplayName(...)` for weapon-level-up and acquisition toasts.
- Reflected `applyItemWorldSceneCompletionRewards` usage in `memory/wiki/features` entries for `WorldAnvilItemWorldFlowRuntime` and `WorldPortalItemWorldFlowRuntime`, and clarified callback ownership in `WorldScenePortalItemWorldFlowRuntime`.

## R1 item-world return polish (2026-06-04 continuation)

- Moved Item World return fade preparation out of a one-off scene method by wiring `ItemWorldReturnFadeRuntime` with `normalizeWorldVisuals`.
- `WorldItemWorldSceneFlowRuntime` callers now use `itemWorldReturnFade.start()` directly, while `ItemWorldReturnFadeRuntime` performs the world-visual normalization callback before starting fade-in.
- `WorldItemWorldSceneFlowRuntime` was further decoupled from `LdtkWorldScene` by injecting a `popScene` callback instead of calling `sceneManager.pop()` internally via `Game`.

## Kill Ï≤òÎ¶¨ Í≤ΩÎ°ú Ï†ïÌï©ÏÑ± (2026-06-04 continuation)

- `LdtkWorldScene` Ï†Å ÏóÖÎç∞Ïù¥Ìä∏ÏóêÏÑú Ï£ΩÏùå ÏßÅÌõÑ `worldEnemyKillRuntime.handle(enemy)`Î•º ÏßÅÏ†ë Ìò∏Ï∂úÌïòÏßÄ ÏïäÍ≥†, ÌîÑÎ†àÏûÑ Îã®ÏúÑ `processDefeatedEnemies(this.enemies)` Í≤ΩÎ°úÎ°ú ÏùºÏõêÌôî.
- `WorldEnemyKillRuntime.processDefeatedEnemies`ÏóêÏÑú `enemy.shouldRemove` ÏÑ†Í∞ÄÎìúÎ•º Ï†úÍ±∞Ìï¥ `_postDefeatHandled` Í∏∞Î∞ò 1Ìöå Ï≤òÎ¶¨ Í≤ΩÎ°úÍ∞Ä ÎèÑÌä∏/Í≥µÍ≤©/ÌôòÍ≤Ω ÏÇ¨Îßù Î™®ÎëêÏóêÏÑú ÏùºÍ¥ÄÎêòÍ≤å Ï†ÅÏö©ÎêòÎèÑÎ°ù Ï†ïÎ¶¨.
## World ∑±≈∏¿” ¿˙¿Âº“ ¿«¡∏º∫ ¡§«’º∫ ¡§∏Æ (2026-06-04 continuation)

- `WorldEgoDialogueRuntime`, `InventoryTutorialHintRuntime`, `WorldPortalItemWorldFlowRuntime`, `WorldEdgeTransitionFlowRuntime`, `WorldPlayerSpawnRuntime`, `WorldPrologueEndRuntime`, `WorldSacredPickupRuntime`, `WorldSpawnState`ø°º≠ `sacredSave` ¡˜¡¢ import∏¶ ¡¶∞≈«œ∞Ì scene callback ¡÷¿‘¿∏∑Œ ¿¸»Ø.
- ¥©∂Ùµ» `WorldEgoDialogueRuntime`, `WorldPrologueEndRuntime` ±‚¥… ∆‰¿Ã¡ˆ∏¶ Ω≈±‘ ª˝º∫«œ∞Ì ∫Ø∞Êµ» save-state ∞Ëæ‡ «◊∏Ò/∞Ê∞Ë ±‘ƒ¢¿ª ¿ß≈∞ø° ¡§«’»≠.
- `memory/wiki/features/RefactorRoadmap.md` π◊ `memory/wiki/WIKI_INDEX.md`ø° ¡¯«‡ ªÛ≈¬øÕ ∏µ≈©∏¶ π›øµ.

## Item World return choreography hardening (late continuation, 2026-06-04)

- Tightened anvil-return completion choreography in game/src/scenes/world/WorldAnvilItemWorldFlowRuntime.ts:
  - onComplete is now routed through shared completion lifecycle helper with a one-shot guard (hasCompleted).
  - onPrologueEnd is now guarded as one-shot to prevent duplicate chapter-transition callbacks on repeated prologue-end signals.
- Fixed WorldAnvilItemWorldFlowRuntime.enterFromTunnel() to honor entryCorridor from edge transition callers, so non-default tunnel/right-edge transitions can enter ItemWorldScene with corridor flow when required.
