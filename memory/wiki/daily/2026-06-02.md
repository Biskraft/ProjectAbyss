# 2026-06-02

- Fixed default anvil growth entry hitch/floating-floor regression. `LdtkWorldScene` now prewarms the hidden Level 36 `ItemWorldGhostOverlay` during zoom, activates it at zoom completion, and immediately reveals the 6-tile spawn neighborhood so the player does not stand on invisible collision while nearby terrain scales in.
- Started official-release refactor on `official/stage-1` after preserving `vertical-slice` / `vslice-1.0`. Added `RuntimeCollisionScope` and moved the default Item World entry's full-world collision clear, ghost extension, and Level 36 collision stamping behind that scoped transaction. Verification: `npx tsc --noEmit` and `npm run build` from `game/` passed.
- Extracted `ItemWorldEntryStreamRuntime` for official Item World entry geometry: Level 36 ghost placement, LDtk `Player` start resolution, fallback start resolution, and growth-zoom player projection. `LdtkWorldScene` still orchestrates rendering/collision but no longer owns those calculations. Verification: `npx tsc --noEmit` and `npm run build` from `game/` passed.
- Browser smoke on the official branch dev server passed at `http://127.0.0.1:5178/play/?debug=1`: HTTP 200, one 640x360 canvas, no hard browser errors. Later the favicon 404 was removed by adding an inline SVG favicon.
- Split the official anvil growth state machine into `ItemWorldEntrySequence`. `AnvilDeploymentFactory` now constructs that class directly, `LdtkWorldScene` types `itemDeployment` as the sequence, and `ItemDeploymentController.ts` is reduced to legacy laser plus compatibility re-export. Verification: `npx tsc --noEmit`, `npm run build`, and dev-server browser smoke passed.
- Split captured-world growth rendering into `ItemWorldGrowthSnapshotController`. `LdtkWorldScene` now only starts/updates/destroys the controller and asks it for projection data; RenderTexture capture, item sprite overlay, and source-visual hiding/restoration are no longer scene-owned. Verification: `npx tsc --noEmit`, `npm run build`, and puppeteer smoke passed.
- Split Level 36 ghost overlay construction into `ItemWorldGhostStreamOverlay` and stream collision/camera extension into `ItemWorldGhostCollisionRuntime`. `LdtkWorldScene` still orchestrates filters/attachment, but row-length restore and ghost collision restore state moved out. Verification: `npx tsc --noEmit`, `npm run build`, and puppeteer smoke passed.
- Moved preserved laser deployment implementation into `LegacyLaserItemDeploymentController.ts`; `ItemDeploymentController.ts` is now a facade for old imports plus option type exports. Verification: `npx tsc --noEmit`, `npm run build`, and puppeteer smoke passed.
- Split Item World entry asset warming into `ItemWorldEntryPreloader` and `ItemWorldScene` push/reveal fade into `ItemWorldEntryPushTransition`. `LdtkWorldScene` now delegates prestream calls and uses the transition object's `isActive` guard instead of owning another transition boolean. Verification: `npx tsc --noEmit`, `npm run build`, and puppeteer smoke passed.
- Consolidated procedural `ItemWorldScene.onComplete` base return handling in `LdtkWorldScene.completeItemWorldSceneReturn()`, keeping path-specific reward/dialogue order intact while sharing pop/fade/tutorial/hint/gold handling. Also added `pushPreparedItemWorldScene()` for the repeated overworld-hide/UI-detach/visual-release/camera-reset push prep. Verification: `npx tsc --noEmit`, `npm run build`, and puppeteer smoke passed.
- Fixed release HTML asset noise: `index.html` now references the public Press Start 2P font as `/fonts/PressStart2P.woff2`, which removes Vite's unresolved font warning, and defines an inline SVG favicon to avoid `/favicon.ico` 404s. Verification: `npm run build` has no font warning, and puppeteer smoke confirms the font is loaded plus the data favicon is present.
- Shared LDtk-authored tileset path collection through `@level/LdtkTilesetPaths` so `ItemWorldEntryPreloader` and `ItemWorldScene` keep the same authored-tileset preservation behavior. Verification: `npx tsc --noEmit` and `npm run build` from `game/` passed.
- Extracted first Item World return inventory hint state into `InventoryTutorialHintRuntime`, keeping anvil-deferred hint flushing and Rustborn equip cleanup out of `LdtkWorldScene` fields. Verification: `npx tsc --noEmit`, `npm run build`, and puppeteer smoke passed.
- Extracted deployment tunnel collision/tile clearing into `ItemDeploymentTunnelRuntime`, including overworld restore cells and active-builder tunnel restore cells. Verification: `npx tsc --noEmit`, `npm run build`, and puppeteer smoke passed.
- Extracted Level 36 ghost reveal movement tracking into `ItemWorldGhostRevealRuntime`, so `LdtkWorldScene` no longer owns last-player-position/activated reveal fields. Verification: `npx tsc --noEmit`, `npm run build`, and puppeteer smoke passed.
- Extracted Level 36 ghost stream orchestration into `ItemWorldGhostStreamRuntime`, moving overlay lifetime, pending delayed builds, entrance/start state, prewarm/load activation, and item-palette rim attachment out of `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and puppeteer smoke passed.
- Extracted fixed handcrafted Item World active state into `FixedItemWorldRuntime` and changed `InventoryTutorialHintRuntime` to read current `Inventory`/`unlockedEvents` via getters so respawn/save-load replacement cannot leave stale references. Verification: `npx tsc --noEmit`, `npm run build`, and puppeteer smoke passed.
- Moved used-anvil return snapshot/level/item/retire flag into `AnvilItemWorldReturnState`, replacing `lastUsedAnvil*` scene fields while preserving return placement and deferred inventory hint behavior. Verification: `npx tsc --noEmit`, `npm run build`, and puppeteer smoke passed.
- Moved pending portal entry data/entity cleanup into `PortalEntryRuntime`, removing `pendingPortalData` and `pendingPortalEntity` from `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and puppeteer smoke passed.
- Moved portal/anvil-dive Item World peeling transition lifecycle into `ItemWorldTransitionRuntime`, removing duplicated `ItemWorldTransitionController` construction and dead `PortalTransition` state from `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and puppeteer smoke passed.
- Moved world portal entity lifetime and overlap/update scanning into `PortalRuntime`, removing the direct `portals` list from `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and puppeteer smoke passed.
- Moved save point lists, proximity hint state, delayed save queue, nearest-save snapping, and deployment prompt hiding into `SavePointRuntime`; `LdtkWorldScene.performSave()` still owns the save payload. Verification: `npx tsc --noEmit`, `npm run build`, and puppeteer smoke passed.
- Removed leftover altar wrapper methods from `LdtkWorldScene`; callers now use `WorldAltarController` directly for update, spawn, clear, close, and redraw. Verification: `npx tsc --noEmit`, `npm run build`, and puppeteer smoke passed.
- Shared the submerged oxygen vignette/bar through `OxygenOverlay`, removing duplicated `oxygenOverlay`/`oxygenBar` graphics from World and Item World scenes and destroying the external UI graphics on scene teardown. Verification: `npx tsc --noEmit`, `npm run build`, and puppeteer smoke passed.
- Shared gamepad connect/disconnect toast wiring through `attachGamepadToast()`, replacing duplicated callbacks in World, LDtk World, and Item World scenes while preserving each scene's `_gpUnsub` teardown. Verification: `npx tsc --noEmit`, `npm run build`, and puppeteer smoke passed.
- Updated `Sheets/tools/validate.mjs` so intentional LDtk-authored override tilesets are allowlisted and checked for file existence instead of reported as CSV divergence. `node Sheets/tools/validate.mjs` and `npm run build` now report 0 CSV integrity warnings; puppeteer smoke passed.
- Set an explicit 1024 KB Vite scene chunk warning budget in `game/vite.config.ts` and documented it in `features/BuildChunkBudget.md`; this records the current manual scene chunk policy instead of leaving the Item World chunk as a normal-build warning. Verification: `npm run build` passed without build warnings.
- Extracted LDtk HUD minimap rendering into `WorldMinimapRuntime`, including player-dot projection, combat opacity, and active-builder dynamic IntGrid overlay. `WorldUiController` now reads the minimap through a getter so redraws do not leave stale container references. Verification: `npx tsc --noEmit` and `npm run build` passed.
- Extracted LDtk world-map overlay sync into `WorldMapRuntime`, including exploration state, markers, player position, debug-map setup, and active-builder dynamic IntGrid grids. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` browser smoke with `M` toggled passed.
- Extracted debug-only world warp input into `WorldDebugWarpRuntime`, moving Backquote click-to-warp, ESC cancel, Shift+M debug map entry, hint text, cursor, and pointer listener cleanup out of `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` browser smoke with Backquote/click/ESC/Shift+M passed.
- Extracted the LDtk world custom game-over overlay and respawn input into `WorldGameOverRuntime`; save-load recovery remains in `LdtkWorldScene.respawnPlayer()`. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` browser smoke passed.
- Extracted save-room BGM dimming into `SaveRoomAudioRuntime` and cleared-item re-dive confirmation UI/input into `AnvilCyclePromptRuntime`, leaving `LdtkWorldScene` with only orchestration callbacks for item placement and altar return. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` browser smoke passed.
- Shared the Item World leave-confirm modal through `ItemWorldLeaveConfirmPanel`, replacing duplicate panel construction in `ItemWorldUiController` and the LDtk frozen-return confirm path. Verification: `npm run build` passed with 0 CSV warnings, and `/play/?debug=1` browser smoke passed.
- Extracted LDtk world one-time movement/combat tutorial hint state into `WorldTutorialHintRuntime`; `LdtkWorldScene` now keeps only shared `TutorialHint` hydration/save ownership. Verification: `npm run build` passed with 0 CSV warnings, and `/play/?debug=1` browser smoke passed.
- Extracted anvil deployment growth blur state into `WorldDeployBlurRuntime`, moving blur filter lifetime, ramp timing, and target append/remove out of `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` browser smoke passed.
- Shared the low-HP heal tutorial cue through `LowHpHealHintRuntime`, removing duplicate `PlayerSave` one-shot checks from LDtk World and Item World scenes. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` browser smoke passed.
- Extracted legacy laser desaturation filter state into `WorldLaserDesaturationRuntime`; `LdtkWorldScene` still orchestrates dungeon atmosphere activation and pending ghost tunnel scheduling. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` browser smoke passed.
- Extracted LDtk world no-weapon attack feedback cooldown into `WorldNoWeaponFeedbackRuntime`, leaving `LdtkWorldScene` with only the player/toast dependency wiring. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` browser smoke passed.
- Extracted LDtk Item World return fade overlay/timer into `ItemWorldReturnFadeRuntime`; world visual normalization remains scene-owned. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` browser smoke passed.
- Extracted LDtk title handoff fade, first `Shaft_01` area-title queue, and initial HUD/minimap reveal gate into `WorldIntroHandoffRuntime`. Save-data entry now explicitly skips only the intro gate while preserving the overlay fade. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` browser smoke on port 5178 passed.
- Shared LDtk-authored camera-zone activation through `CameraZoneRuntime`, removing duplicated zone state/update loops from `LdtkWorldScene` and `ItemWorldScene` while preserving LDtk world's specific-zone priority and Giant Builder suppression. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` browser smoke passed.
- Extracted LDtk world boss arena temporary collision locks into `WorldBossLockRuntime`, including invisible lock-door lifecycle and boss fight start/clear telemetry pairing. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` browser smoke passed.
- Extracted LDtk ending trigger storage, `EndingSequence` update, save deletion, camera reset, and `EndingScene` replacement into `WorldEndingRuntime`. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` browser smoke passed.
- Shared boss HP bar reveal/update policy through `BossHpRuntime`, removing duplicate `_bossBarShown`/engaged checks from LDtk World and Item World scenes while leaving explicit hide boundaries scene-owned. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` browser smoke passed.
- Extracted LDtk-authored `Dialogue`/`Memory` trigger storage, interact prompt positioning, and trigger completion writes into `WorldDialogueTriggerRuntime`; `LoreDisplay` remains shared scene-owned UI for code-driven dialogue flows. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` browser smoke passed.
- Extracted LDtk IntGrid void contact fade/input-lock/cooldown state into `WorldVoidRuntime`; `LdtkWorldScene` still owns builder-aware safe return-point resolution and level reload placement. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` browser smoke passed.
- Extracted LDtk world exit glow and builder entrance glow lifetime/update into `WorldExitGlowRuntime`; `LdtkWorldScene` now only wires player center/entity layer and keeps builder transform sync. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` browser smoke passed.
- Extracted LDtk edge-transition fade/pending state into `WorldEdgeTransitionRuntime`; edge detection and level loading stay scene-owned, while fade state, pending direction/level, item-world sentinel, and spawn hint tiles moved out. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` browser smoke passed.
- Extracted LDtk relic/HP acquire modal ownership into `WorldAcquireOverlayRuntime`, including overlay attachment order, blocking `ATTACK` consumption, and HUD/minimap hide-restore policy. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` browser smoke passed.
- Removed dead archived `MemoryDive`/`ScreenCrack` wiring from `LdtkWorldScene`: no caller started `runDiveTransition()`, `diveTransitionActive` had no true path, and the scene only retained per-frame checks/cleanup. Effect classes remain archived. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` browser smoke passed.
- Extracted frozen-snapshot return interaction UI into `WorldFrozenReturnRuntime`, moving the proximity handler, return prompt, leave-confirm panel, confirm/cancel input, and white return fade ticker out of `LdtkWorldScene` while leaving atmosphere filters and snapshot render ownership scene-side. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` browser smoke passed.
- Extracted procedural Item World room-to-room fade state into `ItemWorldRoomTransitionRuntime`; `ItemWorldScene.transitionState` now keeps only non-room flows (`exit_fade`, `post_clear_hold`) while prompt suppression still treats active room fades as blocking. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` browser smoke passed.
- Extracted Item World world-anchored KeyPrompt placement into `ItemWorldWorldPromptRuntime`; trapdoor and ItemStratum anvil prompts now share the same UI-layer lifetime and camera projection path. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` Puppeteer smoke passed.
- Extracted final Item World clear absorb/dissolve ownership into `ItemWorldAbsorbDissolveRuntime`, including grayscale/contrast filter lifetime, 1000 ms absorb timing, `WorldPullInTransitionController`, and trapdoor sprite fallback reparent before exit fade. Also removed dead legacy `descent_fall` timer/camera state from `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` Puppeteer smoke passed.
- Extracted Item World entry corridor visibility/filter ownership into `ItemWorldEntryCorridorVisibilityRuntime`, moving normal-world layer hiding/restoration, corridor background filter, and post-corridor color-restore filter state out of `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` Puppeteer smoke passed.
- Extracted Item World entry corridor platform reveal ownership into `ItemWorldEntryCorridorRevealRuntime`, moving reveal-node registration, top-left/center coordinate handling, and scale reveal animation out of `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` Puppeteer smoke passed.
- Extracted pure Item World entry corridor layout calculations into `ItemWorldEntryCorridorLayout`, including LDtk corridor identifier parsing, deterministic opening/tail selection, stitched collision composite creation, bottom-exit Y, and entry spawn resolution. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` Puppeteer smoke passed.
- Extracted Item World entry corridor visual container ownership into `ItemWorldEntryCorridorVisualRuntime`, moving LDtk corridor wall rendering, generic solid sprite substitution, fallback platform graphics, grayscale/contrast filter application, and visual cleanup out of `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` Puppeteer smoke passed.
- Extracted MemoryShardNPC capture seal-orb VFX into `ItemWorldCaptureOrbRuntime`, moving orb Graphics lifetime, homing update, arrival flash callback, and teardown cleanup out of `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` Puppeteer smoke passed.
- Extracted procedural Item World developer overlay/key ownership into `ItemWorldDevOverlayRuntime`, moving RoomGraph debug overlay mounting, Shift+2 toggle, top-left topology label, Shift+L topology cycle, and listener cleanup out of `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` Puppeteer smoke passed.
- Extracted procedural Item World stratum weather ownership into `ItemWorldWeatherRuntime`, moving `WeatherSystem` lifetime, area-palette lookup, temperament profile mapping, full-grid collision binding, camera-view updates, and teardown cleanup out of `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` Puppeteer smoke passed.
- Extracted procedural Item World re-entry stratum picker UI into `ItemWorldStratumPickerRuntime`, moving picker container lifetime, selection state, pulse redraw, row/detail rendering, control hints, and modal input handling out of `ItemWorldScene` while keeping `jumpToStratum()` scene-owned. Verification: `npx tsc --noEmit`, `npm run build`, and `/play/?debug=1` Puppeteer smoke passed.
- Extracted ItemStratum LDtk Anvil interaction into `ItemWorldAnvilRuntime`, moving anvil entity lifetime, proximity prompt projection, prompt suppression, and return-request input consumption out of `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted Item World Trapdoor/FloatingItemDrop interaction into `ItemWorldTrapdoorRuntime`, moving proximity prompt projection and `ATTACK` consumption out of `ItemWorldScene` while keeping descent/clear sequencing scene-owned. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted procedural Item World HUD presentation into `ItemWorldHudRuntime`, moving item EXP panel setup, floor/debug text refresh, and depth gauge cleared-state calculation out of `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted procedural Item World camera follow into `ItemWorldCameraRuntime`, moving player map-bound clamping and idle look-hold camera input out of `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted procedural Item World container lift prompt into `ItemWorldContainerPromptRuntime`, moving prompt container lifetime and cleanup out of `ItemWorldScene` while leaving grab/carry physics scene-owned. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted procedural Item World throwable-container carry state into `ItemWorldContainerCarryRuntime`, moving held/pulling container state, pull interpolation, prompt update, and arc tether update out of `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted procedural Item World onboarding message/input handling into `ItemWorldOnboardingRuntime` and removed the unused hidden `ControlsOverlay` instance from `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted procedural Item World leave-confirm modal payload and MENU/ATTACK/DASH/JUMP input handling into `ItemWorldEscapeRuntime`, leaving exit fade sequencing scene-owned. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted procedural Item World non-final boss continue/exit choice input into `ItemWorldBossChoiceRuntime`, leaving actual stratum continue/exit sequencing scene-owned. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted unified Item World `StratumClearOverlay` payload and `post_clear_hold` input handling into `ItemWorldStratumClearRuntime`, leaving continue/exit sequencing scene-owned. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted legacy A16 Item World stratum-clear stats panel wrapper into `ItemWorldStratumClearPanelRuntime`, moving panel payload copying and ATTACK confirm consumption out of `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted procedural Item World exit-fade overlay timing and alpha ramp into `ItemWorldExitFadeRuntime`, leaving high-level transition state and final return sequencing scene-owned. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted procedural Item World debug/enchant input routing into `ItemWorldDebugInputRuntime`, moving Shift+debug terrain/cheat/container controls and 1/2/3 enchant selection out of `ItemWorldScene` while keeping terrain mutation callbacks scene-owned. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted procedural Item World Ego Shard cast input into `ItemWorldEgoShardCastRuntime`, moving charge state, cast preview/spawn, cast gap, and recovery queue ticking out of `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted procedural Item World Ego Shard projectile update into `ItemWorldEgoShardProjectileRuntime`, moving flight update, solid binding, container-fluid post-flush, and player-proximity retrieval out of `ItemWorldScene` while keeping impact and hit callbacks scene-owned. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted Item World map/inventory disabled-input feedback into `ItemWorldUnavailableInputRuntime`, preserving action consumption and `toast.currently_unavailable` while removing another input policy block from `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted the player/enemy movement VFX and fluid-residue feedback portion of `ItemWorldScene.updateMovementVfx()` into `ItemWorldMovementVfxRuntime`, leaving throwable-container physics, Ego Shard projectile update, and low-HP/item-pickup overlay updates scene-side for a future container boundary. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted procedural Item World throwable-container physics into `ItemWorldContainerPhysicsRuntime`, moving container body ticks, environment impact handling, enemy/player overlap resolution, thrown-container enemy hit dispatch, and container-fluid flush out of `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted procedural Item World pickup, hostile projectile, and enemy body-contact update paths into `ItemWorldPickupRuntime`, `ItemWorldProjectileRuntime`, and `ItemWorldEnemyContactRuntime`. Drop/reward creation and boss clear sequencing stay scene-owned. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted procedural Item World static entity/tile hazard update into `ItemWorldStaticEntityRuntime`, moving spike damage, collapsing/growing/item-display/locked-door updates, and player sword interactions with cracked floors, breakable props, switches, and containers out of `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted LDtk `Memory` trigger visual/lore activation into `ItemWorldMemoryTriggerRuntime`, removing the scene-owned `memoryTriggers` collection while keeping Memory Room placement and MemoryShardNPC rewards scene-owned. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted Item World memory resident lifetime and proximity Ego dialogue into `ItemWorldResidentRuntime`, removing the scene-owned `memoryResidents` collection while keeping hub spawn-point policy and resident layer ownership scene-side. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted Item World player attack feedback and enemy kill reward/removal processing into `ItemWorldEnemyCombatRuntime`, leaving enemy spawning and boss portal/trapdoor sequencing in `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted procedural Item World tile hazard/burnable/fluid simulation into `ItemWorldTileHazardRuntime`, moving active tile bounds, `TileMutator` ticking, burnable cleanup, player/enemy tile hazard damage, and fluid system updates out of `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted throwable-container fluid paint/contact behavior into `ItemWorldContainerFluidRuntime`, moving splash IntGrid painting, water/magma solidification, acid container-chain exposure, fluid refresh dirty state, and cryo connected-fluid freeze out of `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted Ego Shard terrain impact and debug elemental sweep behavior into `ItemWorldEgoShardImpactRuntime`, leaving projectile flight, enemy/container hit checks, and shard retrieval scene-side. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Moved Item World thrown-container enemy hit dispatch and player-pushed container X-occupancy checks from `ItemWorldScene` into `ItemWorldContainerPhysicsRuntime`, matching the documented container physics boundary while keeping container VFX/fluid callbacks scene-side. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted Ego Shard enemy/container hit callbacks into `ItemWorldEgoShardCombatRuntime`, leaving projectile flight in `ItemWorldEgoShardProjectileRuntime`, terrain impact in `ItemWorldEgoShardImpactRuntime`, and container VFX/fluid wrappers scene-side. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted lazy per-cell Item World runtime spawning into `ItemWorldRuntimeCellSpawner`, moving active-cell LDtk containers, procedural container spawners, fluid spawners, burnable zones, duplicate-cell tracking, and initial container settling out of `ItemWorldScene`. Removed the dead eager full-map runtime spawn branch that was permanently disabled. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted Item World LDtk static entity creation into `ItemWorldStaticEntitySpawner`, keeping arrays/clear/update scene-owned while moving `Building`, `Spike`, `CrackedFloor`, `CollapsingPlatform`, `GrowingWall`, `Switch`, `LockedDoor`, `ItemDisplay`, and Memory/Camera/Anvil forwarding out of the large scene method. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted designer `ItemSpawner` reward creation into `ItemWorldRoomRewardSpawner`, keeping pickup lifecycle scene-owned while moving deterministic gold/healing reward rolls, rarity/depth gold scaling, and legendary/ancient bonus ember creation out of `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted Item World breakable-prop destruction side effects into `ItemWorldBreakablePropRuntime`, moving shatter VFX, `breakable_destroy` SFX, sword hitstop/shake/sparks, gold burst drops, and flask drops out of `ItemWorldScene` while keeping break timing and lists scene-owned. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted throwable-container destruction VFX/SFX into `ItemWorldContainerDestructionRuntime`, leaving fluid paint/effect handling in `ItemWorldContainerFluidRuntime` and break timing in the caller runtimes. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Moved Shift+G debug throwable-container creation into `ItemWorldDebugInputRuntime`; `ItemWorldScene` now only supplies the player, container list, and entity layer for that debug path. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke with Shift+G, and `git diff --check` passed with only existing line-ending warnings.
- Removed the dead `ItemWorldScene.createEnemyFromType()`/`applyCycleScaling()` wrapper now that cycle scaling is applied by bumping the spawn level directly before calling `ItemWorldSpawnController.createEnemyFromType()`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted Item World enemy spawn context, placement, and room registration into `ItemWorldEnemySpawnRuntime`, moving spawn-point context creation, deterministic position picking, boss flat-floor lookup, `setEnemyRoomKey()`, enemy list/layer attachment, and `roomEnemyCount` increments out of `ItemWorldScene.spawnEnemiesInRoom()`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted shared non-combat room-clear state mutation into `ItemWorldRoomClearRuntime`, replacing repeated `cell.cleared`, `roomsCleared++`, optional Recovery, and `persistRoomState()` blocks in `ItemWorldScene.spawnEnemiesInRoom()`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted MemoryShardNPC replacement spawn and capture callback ownership into `ItemWorldMemoryShardSpawnRuntime`, moving innocent creation, NPC registration, capture text/HUD refresh, flash/SFX/orb VFX, and first shard-recall Ego dialogue unlock out of `ItemWorldScene.spawnEnemiesInRoom()`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted Item World safe-room ambient resident placement into `ItemWorldSafeRoomResidentSpawnRuntime`, moving deterministic hub spawn-point selection, solid-floor filtering, resident shuffle, jitter, and variant selection out of `ItemWorldScene.spawnEnemiesInRoom()`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted combat-room enemy selection into `ItemWorldEnemyEncounterRuntime`, moving CSV spawn-table lookup, cycle level offset, treasure/boss/normal enemy creation, weighted normal entry count rolling, MemoryShard replacement chance, and stratum/distance stat scaling out of `ItemWorldScene.spawnEnemiesInRoom()`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Removed dead Item World room-by-room runtime code now superseded by the full-map path: `loadRoom()`, door-trigger transition wiring, legacy code-template fallback room loading, legacy `spawnEnemies()`/`spawnBoss()`, door markers, and unused wrapper helpers. Restored the still-live full-map helpers (`preSpawnNeighborRooms()`, `getInteriorTilesForRoom()`, `applyItemWorldAreaTileset()`) after the bounded deletion pass. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted Item World lazy cell visual rendering into `ItemWorldCellVisualRuntime`, moving cell visual records, rendered layer tracking, visible-window key state, aggregate child destruction, LDtk tile filtering/substitution, visual bleed, and lazy cell teardown out of `ItemWorldScene`. `ItemWorldScene` still owns full-grid collision construction and aggregate filter setup. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted LDtk ItemStratum template selection into `ItemWorldTemplatePickerRuntime`, moving required-exit matching, memory-room override validation, role-to-room-type policy, fallback ranking, and missing-template warnings out of `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted Item World stitched full-grid collision assembly into `ItemWorldFullGridRuntime`, moving LDtk room collision copy, closed vertical-exit 2-tile solid sealing, and outer full-map boundary collision out of `ItemWorldScene.buildFullMap()`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Moved designer `ItemSpawner` point capture into `ItemWorldRoomRewardSpawner`, removing the duplicated `roomItemSpawners` map and hardcoded tile-size conversion from `ItemWorldScene.buildFullMap()`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Removed dead Item World door-mask carve/seal wrappers and unused seal visual helpers from `ItemWorldScene` and `ItemWorldMapController`; stitched collision now only follows LDtk room grids plus `ItemWorldFullGridRuntime`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted Item World full-map boundary frame rendering into `ItemWorldBoundaryVisualRuntime` and removed the no-op `fillNullCellSeal()` calls/definition. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Removed the disabled Item World minimap compatibility shim: no-op `drawMiniMap()`, never-mounted `miniMapContainer`, and stale call sites. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted Item World room-state restore/persist/count helpers into `ItemWorldRoomStateRuntime`, moved initial fullGrid creation into `ItemWorldFullGridRuntime`, and reduced `ItemWorldMapController.ts` to shared geometry constants. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Removed unused Item World entry-corridor cached level/grid/size fields and one-line proxy helpers from `ItemWorldScene`, routing visibility/reveal/static-spawn/debug callbacks directly to their runtimes. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Moved procedural Item World breakable-prop reset/spawn, start-room exclusion, entity-layer attachment, and burnable registration into `ItemWorldBreakablePropRuntime`, leaving `ItemWorldScene.buildFullMap()` with a single runtime call. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted Item World full-map procedural decoration generation into `ItemWorldProceduralDecorRuntime`, moving decorator budget/theme/depth scaling, aggregate attachment, and grass-clump burnable registration out of `ItemWorldScene.buildFullMap()`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted LDtk-authored Item World player spawn capture/resolution into `ItemWorldPlayerSpawnRuntime`, moving the stratum-indexed spawn map and bottom-center to player top-left conversion out of `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted post-template logical room-type assignment into `ItemWorldRoomTypeRuntime`, removing direct `roomTypeMap` ownership from `ItemWorldScene` while preserving Boss/CriticalPath/LDtk room-type priority. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted Item World full-map aggregate layer construction into `ItemWorldFullMapLayerRuntime`, moving render-order container creation, palette/rim filter setup, depth transform, culling flags, and mutation-mask graphics reset out of `ItemWorldScene.buildFullMap()`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Moved Item World player floor-spawn fallback search into `ItemWorldPlayerSpawnRuntime`, so LDtk-authored start spawns and deterministic room-floor fallback now share one runtime boundary. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted Item World Memory Room placement into `ItemWorldMemoryRoomPlacementRuntime`, moving the CSV lookup, deterministic branch-cell selection, and placement map ownership out of `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Moved Item World visible-cell window updates into `ItemWorldCellVisualRuntime`, including lazy render/unload, per-cell runtime spawn callback, aggregate layer visibility, filterArea culling, and fluid refresh callback wiring. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted Item World neighboring-room pre-spawn loops into `ItemWorldNeighborPreSpawnRuntime`, moving four-direction skip/debug/spawn accounting out of `ItemWorldScene` while leaving the actual enemy spawn behavior scene-owned. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Moved Item World mutation/solidified-wall overlay state into `ItemWorldFullMapLayerRuntime`, removing `mutatedCells`, `solidifiedWallCells`, and repaint methods from `ItemWorldScene` while preserving `TileMutator.onWallTileChanged` event wiring. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted room-level Item World spawn policy into `ItemWorldRoomSpawnRuntime`, moving hub/shrine, stratum-start, Memory Room, corridor, Rest, Puzzle, reward, and encounter dispatch rules out of `ItemWorldScene.spawnEnemiesInRoom()`. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Removed dead `ItemWorldScene.rebuildRoomVisuals()` and the now-unused `ItemWorldCellVisualRuntime.destroyAggregateChildren()` helper. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted non-final Item World trapdoor floor-punching into `ItemWorldTrapdoorDescentRuntime`, moving collision-hole carving and erase-blend aggregate overlays out of `ItemWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted procedural Item World Ego dialogue entry state and trigger policy into `ItemWorldEgoDialogueRuntime`, leaving `ItemWorldScene` with compatibility `fireEgo*()` delegates and persistent `egoUnlockedEvents` ownership. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Moved procedural Item World healing/gold pickup arrays, entity-layer attachment, and stratum cleanup into `ItemWorldPickupRuntime`; enemy combat, breakable props, room rewards, and boss rewards now add drops through the runtime API. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Moved procedural Item World hostile projectile array, entity-layer attachment, and cleanup into `ItemWorldProjectileRuntime`; `ItemWorldScene` now only updates the runtime. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Moved Item World switch-triggered locked-door unlock feedback into `ItemWorldStaticEntityRuntime`, removing the scene-owned `unlockDoorByIidLocal()` helper. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `ItemWorldStaticEntityRegistry` for LDtk-authored static entity arrays and destroy/clear lifecycle, removing the scene-owned `spikes`/`crackedFloors`/`breakableProps`/platform/wall/switch/door/building/item-display arrays. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Removed remaining one-line `ItemWorldScene` wrappers around tile bounds, tile hazard tick, breakable/container VFX, Ego Shard impact, static entity spawn, and static entity update; callers now wire directly to the owning runtimes. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `ItemWorldEnemyRegistry` for procedural Item World enemy array lifecycle, moving clear/update/render/defeated-count helpers out of `ItemWorldScene` while keeping spawn, encounter, combat reward, and boss-clear policy in their existing owners. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `ItemWorldContainerRegistry` for throwable-container array lifecycle, full-map settle, clear/reset, and container-top checks, removing the scene-owned `containers` field while preserving carry/physics/fluid/destruction runtime ownership. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `ItemWorldBurnablePropRegistry` for Tier B burnable-prop array lifecycle, removing the scene-owned `burnableProps` field while preserving creation in `ItemWorldRuntimeCellSpawner` and updates in `ItemWorldTileHazardRuntime`. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `ItemWorldRoomSpawnState` for `spawnedRooms`, `roomEnemyCount`, and neighbor pre-spawn repeat tracking, removing those scene-owned fields while preserving spawn/combat policy owners. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `ItemWorldEntryCorridorState` for entry-corridor active/bottom-exit/deferred-dialogue flags, removing the remaining corridor state fields from `ItemWorldScene` while preserving activation, grid handoff, and player placement orchestration. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `ItemWorldRunStats` for earned EXP, earned gold, baseline gold, cleared-room count, and total-room count. `ItemWorldScene.earnedGold` remains as a public getter for `LdtkWorldScene` return payout compatibility. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `ItemWorldFlowState` for non-room `exit_fade`/`post_clear_hold` state, removing the scene-owned `transitionState` field while preserving room-transition, absorb/dissolve, exit-fade, and stratum-clear runtime ownership. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `ItemWorldStratumStartSnapshot` for per-stratum item level/attack/innocent before-values, removing the remaining `stratumStart*` fields from `ItemWorldScene` while preserving stratum-clear and death result payloads. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `ItemWorldEntryGateState` for first-room spawn completion and entry-freeze timing, removing `startSpawnDone` and `entryFreezeTimer` from `ItemWorldScene` while preserving start-room ambient spawn gating and entry freeze behavior. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `ItemWorldTrapdoorState` for final-descent intent and pending floor-punch coordinates, removing `descentToWorld` and `pendingTrap*` fields from `ItemWorldScene` while preserving Trapdoor/FloatingItemDrop entity ownership and clear/absorb sequencing. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Removed dead `lastSafeRoomCol`/`lastSafeRoomRow` writes from `ItemWorldScene`; no live read path remained after the full-map/stratum-spawn refactors. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Added `ItemWorldExitTelemetryState` for the Item World exit analytics duplicate guard, removing the scene-owned `exitTracked` boolean while preserving death-path immediate tracking and normal-return tracking. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Moved the Item World cell-visibility viewport `Rectangle` cache from `ItemWorldScene` into `ItemWorldCellVisualRuntime`, leaving the scene with only the update call and spawn callback. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Moved the remaining Ego entry-dialogue started flag from `ItemWorldScene` into `ItemWorldEgoDialogueRuntime`, preserving the corridor-deferred enter dialogue gate. Verification: `npx tsc --noEmit`, `npm run build`, and `http://localhost:3000/play/?debug=1` Puppeteer smoke passed.
- Extracted LDtk anvil-dive shared UI hide/restore state into `WorldAnvilDiveUiRuntime`, removing `anvilDiveUiHidden` and `anvilDiveUiWasVisible` from `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted LDtk world camera look-hold and post-transition render snap counters into `WorldCameraInputRuntime`; the snap now consumes its 15-frame window instead of staying armed forever after an edge transition. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted LDtk world dungeon-atmosphere grayscale/parallax filters, target list, laser reapply hook, and builder-interior visibility restore state into `WorldDungeonAtmosphereRuntime`. `LdtkWorldScene` still owns frozen snapshot rendering and player reparenting. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted LDtk world frozen player snapshot render copy and distance-driven RGB/grayscale filters into `WorldFrozenSnapshotRuntime`, leaving `WorldFrozenReturnRuntime` with return UI/input and `LdtkWorldScene` with player reparenting. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted LDtk Giant Builder collision stamp list, active stamp set, and tile-origin tracking into `WorldBuilderStampRuntime`; `LdtkWorldScene` now keeps only wrapper calls around active-builder collision stamping. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted LDtk Giant Builder descent camera-shake feedback flag, previous moving state, and step counter into `WorldBuilderStepFeedbackRuntime`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted LDtk Giant Builder interior dissolve alpha state into `WorldBuilderInteriorVisibilityRuntime`, keeping player/interior target detection in `LdtkWorldScene` and applying the same alpha to builder entrance glows. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted LDtk Giant Builder player/carry state into `WorldBuilderPlayerStateRuntime`, moving on-builder/in-builder flags, builder carrier Y velocity, and one-way drop-through grace timing out of `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted LDtk Giant Builder saved positions, route snapshots, run-once keys, active level id, and active mode into `WorldBuilderPersistenceRuntime`, leaving builder object lifetime and route application in `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted LDtk Giant Builder child-entity attachment tracking into `WorldBuilderAttachmentRuntime`, moving attachment lists, local offsets, reparenting, alive checks, and sync callbacks out of `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted LDtk Giant Builder dynamic weather collider caching into `WorldBuilderWeatherRuntime`, removing the scene-owned `weatherBuilderCollider` field and stale collider cleanup from `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted LDtk Giant Builder palette filter construction/application into `WorldBuilderVisualFilterRuntime`, removing builder-specific palette filter fields and `applyBuilderVisualFilters()` from `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted LDtk Giant Builder auxiliary layer attach/sync/teardown and builder filter-target lists into `WorldBuilderLayerRuntime`, removing manual `builderInteriorLayer`/light/leg layer add-position-destroy blocks from `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted LDtk `BuilderSpawner` field parsing and route config calculation into `WorldBuilderSpawnerRuntime`, removing builder-specific string/number/bool field helpers from `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extended `WorldBuilderLayerRuntime` to own main builder body container insertion into the LDtk renderer root, removing `addBuilderToScene()` from `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted Giant Builder procedural grass clump fire registration into `WorldBuilderGrassRuntime`, removing `registerBuilderGrassClumps()` from `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `attachWorldPositioned()` and `attachSizedWorldPositioned()` to `WorldBuilderAttachmentRuntime`, replacing repeated builder entity sync callbacks and removing the `attachToBuilder()` wrapper from `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted visual-only LDtk `BuilderSprite` tile loading/rendering into `WorldBuilderSpriteRuntime`, leaving `LdtkWorldScene.spawnBuilderEntities()` with only the entity-type dispatch call. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `restamp()` and `refreshIfBuilderGrid()` to `WorldBuilderStampRuntime`, then removed the remaining stamp/query wrapper methods from `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Removed deployment-tunnel `stampBuilder`/`unstampBuilder` scene callbacks. `ItemDeploymentTunnelRuntime` now receives `WorldBuilderStampRuntime` directly and calls `restamp()` after active-builder tunnel dig/restore. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Removed remaining `drawMinimap()` / `updateMinimapBuilderLayer()` wrappers from `LdtkWorldScene`; level redraw now calls `WorldMinimapRuntime.draw()` directly and minimap ownership stays in the runtime. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Removed `updateFrozenSnapshotPrompt()` and `showGameOver()` scene wrappers from `LdtkWorldScene`; world update/death handling now calls `WorldFrozenReturnRuntime.updatePrompt()` and `WorldGameOverRuntime.show()` directly. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted maintained LDtk world `ContainerSpawner` refill state into `WorldMaintainedContainerSpawnerRuntime`, removing `maintainedSpawners`, `MAINTAIN_CHECK_MS`, and `tickMaintainedSpawners()` from `LdtkWorldScene` while preserving initial spawn and regular container physics in the scene. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Removed LDtk transition geometry wrappers (`snapToFloor()`, `findEdgePassage()`, `findFloorY()`) from `LdtkWorldScene`; callers now use `WorldTransitionController` directly. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted LDtk world gold/healing pickup arrays and collection update into `WorldPickupRuntime`, removing scene-owned `goldPickups`/`healingPickups` while keeping HealthShard/AbilityRelic and sacred weapon pickup flow scene-owned for now. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted LDtk world `HealthShard` and `AbilityRelic` pickup lifetimes into `WorldRelicPickupRuntime`, removing scene-owned `healthShards`/`relicMarkers` while keeping collected relic state, ability flags, HP bonus, acquire overlays, and analytics as scene callbacks. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted LDtk world `ItemDropEntity` lifetime into `WorldItemDropRuntime`, removing scene-owned `drops` while keeping item creation, inventory mutation, sacred pickup flow, collected item state, and save payload in `LdtkWorldScene` callbacks. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Extracted LDtk world hostile projectile lifetime into `WorldProjectileRuntime`, removing scene-owned `projectiles` while keeping hit sparks, damage numbers, player damage/death handling, hitstop, and camera/screen feedback in `LdtkWorldScene` callbacks. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldEnemyRegistry` for LDtk world enemy array lifecycle, moving add/remove/clear/alive-count ownership out of `LdtkWorldScene` while keeping update, combat, rewards, tile hazards, body contact, spawn policy, and boss lock in the scene. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldContainerRegistry` for LDtk world throwable-container array lifecycle, moving add/remove/clear ownership out of `LdtkWorldScene` while keeping physics, grab/carry, collision, thrown impact, Ego Shard hits, fluid effects, debug spawn policy, and LDtk/ContainerSpawner creation in the scene. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldSpikeRegistry` for LDtk world `Spike` entity lifecycle, moving spike list storage, entity-layer attachment, clear cleanup, builder membership checks, and entity-spike AABB overlap checks out of `LdtkWorldScene` while keeping IntGrid spike checks and player damage/teleport side effects in the scene. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldBreakableRegistry` for LDtk world `Breakable` entity lifecycle, moving breakable list storage, entity-layer attachment, room-clear cleanup, per-frame update, remove-at cleanup, and builder membership checks out of `LdtkWorldScene` while keeping sword-hit detection, shatter VFX/SFX, drops, and pickup side effects in the scene. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldBuildingRegistry` for LDtk world `Building` visual entity lifecycle, moving building list storage, entity-layer attachment, and room-clear cleanup out of `LdtkWorldScene` while keeping LDtk tile-picker validation/spawn decisions in the scene. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldCollapsingPlatformRegistry` for LDtk world `CollapsingPlatform` lifecycle and collision-grid mapping, removing the scene-owned platform list and WeakMap while keeping spawn policy, permanent-collapse persistence, player-on-top checks, and builder stamp refresh in `LdtkWorldScene`. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldBurnablePropRegistry` for LDtk world Tier B `BurnableProp` lifecycle, moving prop list storage, entity-layer attachment, room-clear cleanup, and remove-at cleanup out of `LdtkWorldScene` while keeping BurnableZone parsing, TileMutator registration, ash remnants, and burn policy in the scene. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldBreakablePropRegistry` for LDtk world procedural `BreakableProp` lifecycle, moving prop list storage, entity-layer attachment, update, room-clear cleanup, and remove-at cleanup out of `LdtkWorldScene` while keeping placement exclusions, TileMutator registration, sword/fire destruction policy, drops, and VFX/SFX in the scene. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldSecretWallRegistry` for LDtk world `SecretWall` lifecycle, moving wall list storage, wall-layer attachment, room-clear cleanup, and remove-at cleanup out of `LdtkWorldScene` while keeping persistence replay, collision/tile clearing, reward drops, analytics, toasts, and attack policy in the scene. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldCrackedFloorRegistry` for LDtk world `CrackedFloor` lifecycle, moving floor list storage, entity-layer attachment, room-clear cleanup, and remove-at cleanup out of `LdtkWorldScene` while keeping collision injection/shatter, persistent unlocks, surge/dive/sword policy, feedback, and toasts in the scene. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.
- Added `WorldGrowingWallRegistry` for LDtk world `GrowingWall` lifecycle, moving wall list storage, entity-layer attachment, room-clear cleanup, and remove-at cleanup out of `LdtkWorldScene` while keeping collision injection/shatter, per-frame update, pending slime collection, persistent unlocks, surge/dive policy, feedback, and toasts in the scene. Verification: `npx tsc --noEmit`, `npm run build`, `http://localhost:3000/play/?debug=1` Puppeteer smoke, and `git diff --check` passed with only existing line-ending warnings.

## 세션 — 디자인/내러티브 (오프닝 설계)

### 작업 내용
- 스펙(PLN-SPEC) + 내러티브(시놉시스 v3 / 첫 30분 플로우 / 내러티브 가이드) 검토.
- "시그니처 메카닉을 시작 5분 내 노출" 레퍼런스 조사 (Bloodstained 샤드 / Super Metroid Ceres / Hades / Half-Life / Returnal / Doom 2016 / 사쿠라이 "Start with the Climax").
- 결정: 프롤로그 0막을 플레이 가능 다이브(에코 1지층 간단 전투 → 첫 말소자 유닛 위협 → 둘째 말소자가 위상 찢고 시설 파괴 = Cascade → 아웃)로 승격. 첫 보스는 비교전 위협, 아웃 행위자는 격파 불가 둘째 말소자(두 말소자 분리로 "선택의 결과 부재" 함정 차단). → [DEC-043](../decisions/DEC-043-Prologue-Playable-Dive.md)

### 변경된 파일
- `Documents/Content/Content_Story_Synopsis.md` — v3.1: 프롤로그 P-2 플레이 가능 다이브, P-2.1 첫 말소자 유닛 위협(비교전), P-3 둘째 말소자 찢기/Cascade 재서술
- `Documents/Content/Content_First30Min_ExperienceFlow.md` — "무전투 프롤로그" 폐기, §0막·설계노트·§3·§4 정합, 분량 ~5분

### 다음 단계
- 말소자 위협 연출(정적·사운드 부재·드론 부재 선행) + 위상 찢기 연출 사양 (640×360 가독성)
- 아웃 연출(사운드·카메라·UI) 사양
- 말소자 흰색 언캐니 32px 스프라이트 제작 (Design_Art_Direction §14.4 사양 기반)

## 세션 — 세력 정체성 + 몬스터 재분류 (후속)

### 작업 내용
- 세력 = 디자인 정체성 4그룹 확정(메커닉 아님): 잔존자·인덱서·말소자·드론. → [DEC-044](../decisions/DEC-044-Faction-Visual-Identity.md)
- "모든 몬스터=말소자" 장치 폐기. 드론(slime/skeleton/ghost)=평범한 환경 생물(말소자 아님, Hollow Knight 벌레 격), boss_01≠말소자, 말소자=희귀·특정 이벤트·BLAME! 세이프가드식 흰색 언캐니 호러.
- 프롤로그 정정: "두 말소자" → 단일 말소자 호러 이벤트(비교전). 약한 적(드론) 전투 + 말소자 흰색 호러 등장.
- Design_Art_Direction §14.3(4그룹 비주얼 문법) + §14.4(말소자 흰색 언캐니 32px 사양) 신규 작성.

### 변경된 파일
- `Content_Story_Synopsis.md` (v3.2) · `Content_Characters.md §6` · `Content_First30Min...md` · `Design_World_Master.md §6` — 몬스터 재분류 정합
- `Design_Art_Direction.md` (v3.1) — §14.3/§14.4 세력 비주얼 정체성 신규
- `DEC-043` 프롤로그 단일 말소자화, `DEC-044` 신규

### 잔여
- `System_Enemy_BossDesign.md` 등 타 문서 "몬스터=말소자" 잔존 참조 전수 검증 (gdd-integrity-checker 권장)
