---
date: 2026-05-29
sessions: 2
---

# 2026-05-29 Work Log

## Session 1 - Weapon3DPreview prototype

- Added `Weapon3DPreview` to `game/docs/ui-components.html` after `ItemDetailView`.
- Prototype uses a Three.js low-poly sword rendered into a deliberately low-resolution WebGL buffer, then CSS-upscaled with `image-rendering: pixelated` for a PS2/pixel-art read.
- Scope rule: the preview is for weapon detail/acquire/anvil UI only; existing 2D combat hitboxes and player weapon silhouettes remain authoritative.
- Verification: Playwright CLI captured desktop/mobile screenshots for `file:///C:/Users/Victor/Documents/Works/ProjectAbyss/game/docs/ui-components.html#weapon-3d-preview`; Puppeteer canvas sampling confirmed non-empty WebGL pixels and no console/page errors.

## Session 2 - Erda combo 3 attack2 frames

- `game/src/entities/Player.ts` now plays `attack2` frames `22..25` in forward order for grounded combo 3 (`comboIndex === 2`).
- Combo 1 still uses `attack1` forward and combo 2 still uses `attack1` reverse. Airborne attacks always use `attack_air=26..29` so air combo 3 does not pop to a ground pose. Other shifted strips: `aim=30..33`, `aim_jump=34`, `lift=35..38`.
- Verification: `npx tsc --noEmit` from `game/` passes.
